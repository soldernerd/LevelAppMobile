---
phase: 06-app-icon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - assets/icon/app_icon.png
  - android/app/src/main/res/mipmap-hdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-mdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "assets/icon/app_icon.png exists in the repo (620x620 PNG, the torpedo-level graphic)"
    - "pubspec.yaml dev_dependencies contains flutter_launcher_icons with a ^ version pin"
    - "pubspec.yaml flutter_icons: block is present with image_path, platforms.android adaptive config (foreground + background #0d0a05), and platforms.ios remove_alpha_channel: true"
    - "dart run flutter_launcher_icons exits 0 with no error lines on stdout/stderr"
    - "android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml exists and references adaptive icon layers"
    - "ios/Runner/Assets.xcassets/AppIcon.appiconset/ contains a 1024x1024 PNG (not the Flutter default blue swirl)"
  artifacts:
    - path: "assets/icon/app_icon.png"
      provides: "Source icon for generator"
    - path: "pubspec.yaml"
      provides: "flutter_launcher_icons dev dependency + flutter_icons config block"
    - path: "android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      provides: "Android adaptive icon XML referencing foreground/background layers"
  key_links:
    - from: "pubspec.yaml flutter_icons.image_path"
      to: "assets/icon/app_icon.png"
      via: "flutter_launcher_icons generator"
      pattern: "image_path.*assets/icon/app_icon.png"
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android:icon=\"@mipmap/ic_launcher\""
      via: "existing reference — no change needed"
      pattern: "android:icon"
---

<objective>
Add the custom torpedo-level launcher icon to Android and iOS using flutter_launcher_icons.
A single 620x620 PNG source is copied into the repo, the generator config is added to pubspec.yaml,
and `dart run flutter_launcher_icons` overwrites all platform icon slots.

Purpose: Replace the Flutter default blue swirl with the custom precision-instrument icon before shipping WP1.
Output: All Android mipmap-* PNGs replaced, mipmap-anydpi-v26/ic_launcher.xml created (adaptive icon),
        iOS AppIcon.appiconset populated — observable on Android/iOS home screen.
</objective>

<execution_context>
@C:/Users/lfaes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lfaes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-app-icon/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy source icon + add flutter_launcher_icons config to pubspec.yaml</name>
  <files>assets/icon/app_icon.png, pubspec.yaml</files>

  <read_first>
    - pubspec.yaml — read the full file to see current dev_dependencies format (^ pinning) and the flutter: section structure before editing
  </read_first>

  <action>
    Step 1 — Create the directory and copy the source PNG.
    Create directory assets/icon/ in the Flutter project root if it does not exist.
    Copy C:\Users\lfaes\OneDrive\VisualStudio\LevelApp\docs\Icons\Square310x310Logo_620.png
    to assets/icon/app_icon.png.

    Step 2 — Add flutter_launcher_icons to dev_dependencies in pubspec.yaml.
    The existing dev_dependencies use ^ pinning (e.g. flutter_lints: ^6.0.0, fake_async: ^1.3.3).
    Look up the latest stable version of flutter_launcher_icons on pub.dev and add:
      flutter_launcher_icons: ^<latest_stable>
    Do not add a flutter: assets: section — flutter_launcher_icons reads the file directly via
    its own config path and does not require the asset declared under flutter: assets:.

    Step 3 — Add a flutter_icons: top-level config block to pubspec.yaml (per D-06, D-07, D-08, D-09).
    Place it as a new top-level YAML key after the flutter: section. The block must contain:
      flutter_icons:
        image_path: "assets/icon/app_icon.png"
        platforms:
          android:
            adaptive_icon_foreground: "assets/icon/app_icon.png"
            adaptive_icon_background: "#0d0a05"
            min_sdk_android: 21
          ios:
            remove_alpha_channel: true

    After editing pubspec.yaml, run `flutter pub get` to resolve the new dev dependency.
  </action>

  <verify>
    <automated>dart run flutter pub get</automated>
  </verify>

  <acceptance_criteria>
    - assets/icon/app_icon.png exists and is a valid PNG file (file size > 0 bytes)
    - pubspec.yaml dev_dependencies contains a line matching: flutter_launcher_icons: ^
    - pubspec.yaml contains top-level key flutter_icons:
    - pubspec.yaml contains image_path: "assets/icon/app_icon.png"
    - pubspec.yaml contains adaptive_icon_background: "#0d0a05"
    - pubspec.yaml contains adaptive_icon_foreground: "assets/icon/app_icon.png"
    - pubspec.yaml contains remove_alpha_channel: true
    - pubspec.yaml contains min_sdk_android: 21
    - flutter pub get exits 0 with no error output
  </acceptance_criteria>

  <done>assets/icon/app_icon.png is in place, pubspec.yaml has the flutter_launcher_icons dev dependency and flutter_icons: config block, and flutter pub get resolves cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Run flutter_launcher_icons generator and verify output</name>
  <files>
    android/app/src/main/res/mipmap-hdpi/ic_launcher.png,
    android/app/src/main/res/mipmap-mdpi/ic_launcher.png,
    android/app/src/main/res/mipmap-xhdpi/ic_launcher.png,
    android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png,
    android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png,
    android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml,
    ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json
  </files>

  <read_first>
    - android/app/src/main/res/mipmap-hdpi/ic_launcher.png — note current file size (should change after generator runs)
    - ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json — note current contents (should reference new icon files after generator runs)
  </read_first>

  <action>
    Run the generator from the Flutter project root:
      dart run flutter_launcher_icons

    The generator reads the flutter_icons: config from pubspec.yaml and writes:
    - android/app/src/main/res/mipmap-mdpi/ic_launcher.png (48x48)
    - android/app/src/main/res/mipmap-hdpi/ic_launcher.png (72x72)
    - android/app/src/main/res/mipmap-xhdpi/ic_launcher.png (96x96)
    - android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png (144x144)
    - android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png (192x192)
    - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml (adaptive icon XML)
    - ios/Runner/Assets.xcassets/AppIcon.appiconset/ (all iOS sizes including 1024x1024)

    Do not edit any of these files manually — the generator is the sole source of truth for them.

    After the generator runs, verify the output using the acceptance criteria below.
    If the generator prints any error lines, read the error carefully:
    - "No configuration found" → flutter_icons: block missing or indented incorrectly in pubspec.yaml
    - "Could not find image" → assets/icon/app_icon.png path incorrect or file missing
    Fix and re-run rather than proceeding with a partial output.
  </action>

  <verify>
    <automated>dart run flutter_launcher_icons</automated>
  </verify>

  <acceptance_criteria>
    - dart run flutter_launcher_icons exits 0
    - Generator stdout contains no lines starting with "Error" or "Exception"
    - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml exists
    - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml contains the string "adaptive-icon"
    - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml contains the string "ic_launcher_foreground" or "foreground"
    - android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png exists and has file size > 10000 bytes (192x192 PNG is never tiny)
    - ios/Runner/Assets.xcassets/AppIcon.appiconset/ contains at least one PNG file with "1024" in its name or a PNG > 500000 bytes (the 1024x1024 iOS marketing icon)
    - ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json has been modified (mtime newer than before generator ran, or contents reference new filenames)
  </acceptance_criteria>

  <done>All Android mipmap-* PNGs and mipmap-anydpi-v26/ic_launcher.xml have been overwritten by the generator. iOS AppIcon.appiconset is populated with the custom icon. The app can be built and sideloaded to see the torpedo-level icon on the home screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → pub.dev | flutter_launcher_icons package fetched via flutter pub get |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | flutter_launcher_icons package install | accept | Package is well-established on pub.dev (flutter.dev recommended); version-pinned with ^; no code execution in app bundle — dev tool only |
| T-06-02 | Tampering | source PNG copy from sibling repo path | accept | File copied from local filesystem path under same user account; no network transfer; no executable content in PNG |
| T-06-SC | Tampering | dart pub installs | accept | flutter_launcher_icons is a pub.dev package (not npm/pip/cargo); pub.dev slopcheck protocol does not apply; package is on Flutter's official tooling page |
</threat_model>

<verification>
After both tasks complete, the following must hold:

1. `assets/icon/app_icon.png` exists (PNG, > 0 bytes)
2. `pubspec.yaml` dev_dependencies includes `flutter_launcher_icons: ^`
3. `pubspec.yaml` top-level key `flutter_icons:` is present with correct sub-keys
4. `dart run flutter_launcher_icons` exits 0 (re-runnable safely — idempotent)
5. `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml` exists and contains `adaptive-icon`
6. `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png` file size > 10000 bytes
7. `ios/Runner/Assets.xcassets/AppIcon.appiconset/` contains the 1024x1024 marketing icon PNG
</verification>

<success_criteria>
Phase 6 is complete when:
- The custom torpedo-level icon is present in all Android mipmap-* slots (mdpi through xxxhdpi) and the adaptive icon XML is in mipmap-anydpi-v26/
- The iOS AppIcon.appiconset is fully populated (not the Flutter default blue swirl)
- The app can be built with `flutter build apk` without icon-related errors
- Installing the APK on an Android device shows the custom icon on the home screen launcher
</success_criteria>

<output>
Create `.planning/phases/06-app-icon/06-01-SUMMARY.md` when done.
</output>
