// lib/ui/instrument_screen.dart — stub for Wave 2 compilation.
// Replaced entirely by plan 04-03.
import 'package:flutter/material.dart';

/// Placeholder InstrumentScreen — populated by plan 04-03.
class InstrumentScreen extends StatelessWidget {
  const InstrumentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Instrument Screen — plan 04-03')),
    );
  }
}
