---
phase: 01-data-models-protocol-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - test/widget_test.dart
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "flutter pub get resolves without errors"
    - "pubspec.yaml lists flutter_riverpod: ^3.3.1 and dev dep test: ^1.31.1"
    - "lib/main.dart exists and contains ProviderScope with MockBleManager override"
    - "test/widget_test.dart does not exist (counter app boilerplate deleted)"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Dart package manifest with correct name, org, and Phase 1 deps"
      contains: "flutter_riverpod"
    - path: "lib/main.dart"
      provides: "Minimal ProviderScope stub entry point"
      contains: "ProviderScope"
    - path: "android/app/build.gradle"
      provides: "Android build config (created by flutter create)"
  key_links:
    - from: "lib/main.dart"
      to: "lib/providers/device_provider.dart"
      via: "import of bleManagerProvider"
      pattern: "import.*providers/device_provider"
---

<objective>
Create the Flutter project scaffold and configure pubspec.yaml with Phase 1 dependencies. Wipe the generated counter app boilerplate and replace main.dart with a minimal stub that imports from (not yet created) provider and mock files.

Purpose: Establishes the project structure — pubspec.yaml, lib/, android/, ios/, test/ directories — so that subsequent plans can write Dart source files against a known package layout. The boilerplate wipe prevents the generated widget test from failing flutter test in Wave 4.

Output: pubspec.yaml with correct package name/org and flutter_riverpod + test deps; a minimal lib/main.dart stub ready for Phase 1 content; test/widget_test.dart deleted.
</objective>

<execution_context>
@C:\Users\luke\OneDrive\Claude\get-shit-done\workflows\execute-plan.md
@C:\Users\luke\OneDrive\Claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-models-protocol-parser/01-CONTEXT.md
@.planning/phases/01-data-models-protocol-parser/01-RESEARCH.md
@.planning/phases/01-data-models-protocol-parser/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run flutter create and verify scaffold</name>
  <files>pubspec.yaml, lib/main.dart, android/, ios/, test/widget_test.dart</files>
  <read_first>
    - .planning/phases/01-data-models-protocol-parser/01-RESEARCH.md (section: "Code Examples — flutter create command" for exact invocation and post-create verification steps)
    - .planning/phases/01-data-models-protocol-parser/01-RESEARCH.md (section: "Common Pitfalls — Pitfall 1 and Pitfall 2" for OneDrive and widget_test.dart concerns)
  </read_first>
  <action>
Run `flutter create --org com.soldernerd --project-name inclinometer .` in the project root `C:\Users\luke\OneDrive\VisualStudio\LevelAppMobile`. The directory already contains `.git/`, `CLAUDE.md`, and `.planning/` — flutter create does not touch these.

After running, verify the following files exist: `pubspec.yaml`, `lib/main.dart`, `android/app/build.gradle`, `test/widget_test.dart`. Also confirm that `.planning/PROJECT.md` and `CLAUDE.md` were not modified (run `git status` and inspect).

If flutter create exits with an error due to OneDrive file locks, pause OneDrive sync from the system tray and retry.

This task creates the scaffold only. Do not modify pubspec.yaml or main.dart yet — those are Task 2 and Task 3.
  </action>
  <verify>
    <automated>Test-Path pubspec.yaml, android/app/build.gradle, lib/main.dart, test/widget_test.dart</automated>
  </verify>
  <done>pubspec.yaml, lib/main.dart, android/app/build.gradle, and test/widget_test.dart all exist. .planning/ directory and CLAUDE.md are unmodified (confirmed by git status showing them as clean).</done>
</task>

<task type="auto">
  <name>Task 2: Configure pubspec.yaml with Phase 1 dependencies</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml (read the file as generated by flutter create before modifying)
    - .planning/phases/01-data-models-protocol-parser/01-RESEARCH.md (section: "pubspec.yaml (final Phase 1 state)" for the exact target dependency block)
  </read_first>
  <action>
Edit the generated pubspec.yaml to set:
- `name: inclinometer`
- `description: "Precision inclinometer BLE companion app"`
- `publish_to: 'none'`
- `version: 1.0.0+1`
- Under `dependencies:` add `flutter_riverpod: ^3.3.1` (keep the existing `flutter: sdk: flutter`)
- Under `dev_dependencies:` add `test: ^1.31.1` (keep the existing `flutter_test: sdk: flutter` and `flutter_lints`)
- Under `flutter:` keep `uses-material-design: true`

Do NOT add `flutter_blue_plus`, `permission_handler`, `go_router`, or `wakelock_plus` — these are Phase 2/5 dependencies.

After editing, run `flutter pub get` and confirm it exits 0 with no resolution errors.
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>`flutter pub get` exits 0. pubspec.yaml contains `flutter_riverpod: ^3.3.1` under dependencies and `test: ^1.31.1` under dev_dependencies. No flutter_blue_plus, permission_handler, go_router, or wakelock_plus entries exist.</done>
</task>

<task type="auto">
  <name>Task 3: Wipe boilerplate and write minimal main.dart stub</name>
  <files>lib/main.dart, test/widget_test.dart</files>
  <read_first>
    - lib/main.dart (read the generated counter app content before replacing)
    - .planning/phases/01-data-models-protocol-parser/01-PATTERNS.md (section: "lib/main.dart — Phase 1 minimal stub" for the exact target content)
    - .planning/research/ARCHITECTURE.md (section: "Inject via ProviderScope override" for the canonical pattern)
  </read_first>
  <action>
Delete `test/widget_test.dart` entirely — it imports the counter app widget that will not exist after this wipe.

Replace `lib/main.dart` with a minimal stub. The stub must:
- Import `package:flutter/material.dart`
- Import `package:flutter_riverpod/flutter_riverpod.dart`
- Import `package:inclinometer/ble/mock_ble_manager.dart`
- Import `package:inclinometer/providers/device_provider.dart`
- Define `void main()` that calls `runApp(ProviderScope(overrides: [bleManagerProvider.overrideWith((_) => MockBleManager())], child: const MaterialApp(home: Scaffold(body: Center(child: Text('Phase 1'))))))`

The imports to `mock_ble_manager.dart` and `device_provider.dart` will produce "Target of URI doesn't exist" analyzer warnings at this point — that is expected and acceptable because those files are created in Plans 02, 03, and 04. The stub is a forward declaration of intent.

Do not run `flutter analyze` now — it will report unresolved imports. Analyze is run at the end of Plan 04 after all files are in place.
  </action>
  <verify>
    <automated>dart format --output=none lib/main.dart</automated>
  </verify>
  <done>test/widget_test.dart does not exist. lib/main.dart contains ProviderScope with bleManagerProvider.overrideWith and MockBleManager() override. dart format exits 0 on the file (no syntax errors).</done>
</task>

</tasks>

<verification>
After Plan 01 completes:
- `pubspec.yaml` contains `flutter_riverpod: ^3.3.1` and `test: ^1.31.1`
- `flutter pub get` exits 0
- `test/widget_test.dart` does not exist
- `lib/main.dart` contains ProviderScope with MockBleManager override
- `.planning/` directory and `CLAUDE.md` are unmodified
</verification>

<success_criteria>
Flutter project scaffold exists with correct package name `inclinometer`, org `com.soldernerd`, Phase 1 dependencies resolved, counter app boilerplate removed, and a minimal main.dart stub ready to receive imports from files created in subsequent plans.
</success_criteria>

<output>
Create `.planning/phases/01-data-models-protocol-parser/01-01-SUMMARY.md` when done.
</output>
