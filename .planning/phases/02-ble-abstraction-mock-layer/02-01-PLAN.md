---
phase: 02-ble-abstraction-mock-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/ble/mock_ble_manager.dart
  - test/ble/mock_ble_manager_test.dart
autonomous: true
requirements: [MOCK-01, MOCK-02, MOCK-03, MOCK-04]

must_haves:
  truths:
    - "A unit test that listens to statePackets for 1 second of virtual time receives at least 10 StatePacket events"
    - "All angle_x and angle_y values in those packets are within [-45.0, 45.0]"
    - "A unit test elapsing 10 seconds of virtual time observes battery level decrement by at least 1"
    - "connect() emits ConnectionStatus.connecting immediately, then ConnectionStatus.connected after ~300ms virtual time"
    - "simulateDisconnect() emits ConnectionStatus.disconnected and statePackets goes silent — no new events after the call"
  artifacts:
    - path: "lib/ble/mock_ble_manager.dart"
      provides: "Full MockBleManager implementation replacing all UnimplementedError stubs"
      contains: "StreamController.broadcast()"
    - path: "test/ble/mock_ble_manager_test.dart"
      provides: "Unit tests covering MOCK-01 through MOCK-04"
      exports: []
    - path: "pubspec.yaml"
      provides: "fake_async promoted to direct dev dependency"
      contains: "fake_async"
  key_links:
    - from: "lib/ble/mock_ble_manager.dart"
      to: "lib/ble/ble_protocol.dart"
      via: "StatePacket.encode(_angleX, _angleY, _battery) called in _startTicker()"
      pattern: "StatePacket\\.encode"
    - from: "lib/ble/mock_ble_manager.dart"
      to: "lib/models/device_state.dart"
      via: "ConnectionStatus enum and ScannedDevice model used in stream emissions"
      pattern: "ConnectionStatus\\."
    - from: "test/ble/mock_ble_manager_test.dart"
      to: "lib/ble/mock_ble_manager.dart"
      via: "fakeAsync zone advances virtual clock to trigger Timer.periodic callbacks"
      pattern: "fakeAsync"
---

<objective>
Replace the Phase 1 UnimplementedError stubs in MockBleManager with a production-quality streaming implementation driven by StreamController.broadcast() and Timer.periodic. Add the fake_async dev dependency and a complete test file that verifies all four MOCK requirements using virtual-time tests.

Purpose: Phases 3 and 4 need animated mock streams to build providers and UI against without real hardware. This phase delivers that foundation with full test coverage.
Output: Updated mock_ble_manager.dart, new mock_ble_manager_test.dart, fake_async in pubspec.yaml.
</objective>

<execution_context>
@C:/Users/luke/OneDrive/Claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luke/OneDrive/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ble-abstraction-mock-layer/02-CONTEXT.md
@.planning/phases/02-ble-abstraction-mock-layer/02-RESEARCH.md
@.planning/phases/02-ble-abstraction-mock-layer/02-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from Phase 1 files. -->

From lib/ble/ble_manager.dart — ALL 9 members must remain @override in mock:
```dart
abstract class BleManager {
  Stream<ScannedDevice> get scanResults;
  Stream<ConnectionStatus> get connectionStatus;
  Stream<List<int>> get statePackets;
  Future<void> startScan();
  Future<void> stopScan();
  Future<void> connect(String deviceId);
  Future<void> disconnect();
  Future<void> sendCommand(int commandByte);
  void dispose();
}
```

From lib/ble/ble_protocol.dart — call signatures:
```dart
const int kCmdZeroX = 0x01;
const int kCmdZeroY = 0x02;
static List<int> encode(double ax, double ay, int battery) → List<int>
```

From lib/models/device_state.dart — ConnectionStatus values:
```dart
enum ConnectionStatus { idle, scanning, connecting, connected, disconnecting, disconnected, error }
class ScannedDevice { final String id; final String name; final int rssi; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote fake_async and create test scaffold</name>
  <files>pubspec.yaml, test/ble/mock_ble_manager_test.dart</files>

  <read_first>
    - pubspec.yaml — read lines 39–50 (current dev_dependencies block) before editing so the insertion point is confirmed
    - test/ble/ble_protocol_test.dart — read entire file; this is the canonical test structure analog (imports pattern, main()/group() nesting, package:inclinometer import style)
  </read_first>

  <action>
    Step 1 — pubspec.yaml: Add `fake_async: ^1.3.3` as a direct dev_dependency immediately after `test: ^1.31.0`. Do not change any other line. Run `flutter pub get` after the edit to confirm resolution (fake_async 1.3.3 is already in pubspec.lock as a transitive dep so this should succeed immediately).

    Step 2 — test/ble/mock_ble_manager_test.dart: Create this new file with the Wave 0 scaffold: imports block, a void main() function, and an empty `group('MockBleManager', ...)` body containing one placeholder `test('placeholder', () {})` per requirement group so the file compiles and flutter test discovers it without failing. Use `package:inclinometer/...` import style (never relative paths), matching the analog in test/ble/ble_protocol_test.dart. Import block must include:
    - `import 'dart:math';`
    - `import 'package:fake_async/fake_async.dart';`
    - `import 'package:test/test.dart';`
    - `import 'package:inclinometer/ble/ble_protocol.dart';`
    - `import 'package:inclinometer/ble/mock_ble_manager.dart';`
    - `import 'package:inclinometer/models/device_state.dart';`

    The file must compile even though MockBleManager still has stubs at this point — the scaffold only imports and declares empty test groups, it does not call any MockBleManager methods that would throw.
  </action>

  <verify>
    <automated>flutter pub get && flutter test test/ble/mock_ble_manager_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - pubspec.yaml dev_dependencies block contains the line `fake_async: ^1.3.3`
    - `flutter pub get` exits with code 0 (no dependency conflicts)
    - `flutter test test/ble/mock_ble_manager_test.dart` exits with code 0 (placeholder tests pass)
    - test/ble/mock_ble_manager_test.dart contains the line `import 'package:fake_async/fake_async.dart';`
    - test/ble/mock_ble_manager_test.dart contains the line `group('MockBleManager',`
  </acceptance_criteria>

  <done>pubspec.yaml has fake_async; test scaffold compiles and flutter test exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Implement full MockBleManager</name>
  <files>lib/ble/mock_ble_manager.dart</files>

  <read_first>
    - lib/ble/mock_ble_manager.dart — read entire file; this is the stub being replaced; all 9 @override members must remain
    - lib/ble/ble_manager.dart — read entire file; interface contract that must be fully satisfied
    - lib/ble/ble_protocol.dart — read lines 10–56; kCmdZeroX, kCmdZeroY, and StatePacket.encode() call signatures used in the implementation
    - lib/models/device_state.dart — read entire file; ConnectionStatus enum values and ScannedDevice constructor used in stream emissions
    - .planning/phases/02-ble-abstraction-mock-layer/02-PATTERNS.md — read section "lib/ble/mock_ble_manager.dart"; contains the complete implementation skeleton with all required patterns
    - .planning/phases/02-ble-abstraction-mock-layer/02-RESEARCH.md — read the "Complete MockBleManager Skeleton" code example and all Pitfall entries
  </read_first>

  <action>
    Replace lib/ble/mock_ble_manager.dart entirely. The new implementation must satisfy these concrete requirements (all decisions are locked in CONTEXT.md):

    IMPORTS: `dart:async`, `dart:math`, `package:inclinometer/ble/ble_manager.dart`, `package:inclinometer/ble/ble_protocol.dart`, `package:inclinometer/models/device_state.dart`. Never import flutter_blue_plus.

    FIELDS — final eager-initialized broadcast controllers:
    - `final _scanController = StreamController<ScannedDevice>.broadcast();`
    - `final _statusController = StreamController<ConnectionStatus>.broadcast();`
    - `final _packetController = StreamController<List<int>>.broadcast();`
    - `double _angleX = 0.0; double _angleY = 0.0; int _battery = 85; int _tickCount = 0;`
    - `Timer? _ticker; Timer? _scanTimer;`
    - `final Random _rng;`
    - Constructor: `MockBleManager({Random? random}) : _rng = random ?? Random();`

    STREAM GETTERS: All three return the corresponding controller's stream property.

    startScan(): Emit `ConnectionStatus.scanning` on `_statusController`, then schedule `_scanTimer = Timer(const Duration(milliseconds: 500), ...)` that adds `ScannedDevice(id: 'AA:BB:CC:DD:EE:FF', name: 'Inclinometer', rssi: -65)` to `_scanController` only if `!_scanController.isClosed` (D-06, D-07).

    stopScan(): Cancel `_scanTimer` and null it (D-08).

    connect(String deviceId): Add `ConnectionStatus.connecting` to `_statusController`, then `await Future.delayed(const Duration(milliseconds: 300))`, then reset `_angleX = 0.0; _angleY = 0.0; _tickCount = 0;`, add `ConnectionStatus.connected` to `_statusController`, call `_startTicker()` (D-05, D-12).

    disconnect(): Add `ConnectionStatus.disconnecting` to `_statusController`, call `_stopTicker()`, add `ConnectionStatus.disconnected` to `_statusController` (D-13).

    sendCommand(int commandByte): If `commandByte == kCmdZeroX`, set `_angleX = 0.0`. If `commandByte == kCmdZeroY`, set `_angleY = 0.0`. All other values are silent no-ops (D-09, D-10).

    simulateDisconnect() — NOT @override, concrete method only: Call `_stopTicker()`. If `!_statusController.isClosed`, add `ConnectionStatus.disconnected` to `_statusController` (D-11). Add doc comment: "WP1-only debug escape hatch. Simulates involuntary disconnect. NOT part of [BleManager]."

    dispose() @override: Call `_stopTicker()`. Cancel `_scanTimer`. Close all three controllers in order: `_scanController.close(); _statusController.close(); _packetController.close()` (D-14).

    _startTicker() private: Set `_ticker = Timer.periodic(const Duration(milliseconds: 100), (_) { ... })`. Inside the callback: guard with `if (_packetController.isClosed) return;`. Then update angles: `_angleX = (_angleX + (_rng.nextDouble() - 0.5) * 0.2).clamp(-45.0, 45.0)` and same for `_angleY`. Increment `_tickCount++`. If `_tickCount % 100 == 0`, set `_battery = (_battery - 1).clamp(0, 100)`. Call `_packetController.add(StatePacket.encode(_angleX, _angleY, _battery))` (D-01, D-02, D-03, D-04).

    _stopTicker() private: Call `_ticker?.cancel(); _ticker = null;`.

    CRITICAL PITFALL GUARDS:
    - `.clamp(-45.0, 45.0)` — bounds MUST be double literals (not int). `.clamp(-45, 45)` returns `num`, not `double`, causing a compile error when assigned to `double _angleX`.
    - isClosed guard MUST be the first statement in every Timer callback before any `.add()` call.
    - `simulateDisconnect()` must NOT have `@override` — it is not on the BleManager interface.
  </action>

  <verify>
    <automated>flutter analyze lib/ble/mock_ble_manager.dart && flutter test test/ble/ble_protocol_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter analyze lib/ble/mock_ble_manager.dart` produces zero issues
    - lib/ble/mock_ble_manager.dart does NOT contain `throw UnimplementedError` anywhere
    - lib/ble/mock_ble_manager.dart contains `StreamController<ScannedDevice>.broadcast()`
    - lib/ble/mock_ble_manager.dart contains `StreamController<ConnectionStatus>.broadcast()`
    - lib/ble/mock_ble_manager.dart contains `StreamController<List<int>>.broadcast()`
    - lib/ble/mock_ble_manager.dart contains `Timer.periodic(const Duration(milliseconds: 100)`
    - lib/ble/mock_ble_manager.dart contains `Future.delayed(const Duration(milliseconds: 300))`
    - lib/ble/mock_ble_manager.dart contains `.clamp(-45.0, 45.0)` (double literals, not int)
    - lib/ble/mock_ble_manager.dart contains `void simulateDisconnect()` with NO `@override` annotation
    - lib/ble/mock_ble_manager.dart does NOT import `flutter_blue_plus`
    - Existing Phase 1 tests still pass: `flutter test test/ble/ble_protocol_test.dart` exits 0
  </acceptance_criteria>

  <done>MockBleManager fully implemented; analyzer clean; ble_protocol_test still green</done>
</task>

<task type="auto">
  <name>Task 3: Write test assertions and verify full suite</name>
  <files>test/ble/mock_ble_manager_test.dart</files>

  <read_first>
    - test/ble/mock_ble_manager_test.dart — read entire file; this is the scaffold from Task 1 that receives real assertions
    - lib/ble/mock_ble_manager.dart — read entire file; this is the implementation from Task 2; understand exact method signatures and field names before writing tests
    - .planning/phases/02-ble-abstraction-mock-layer/02-PATTERNS.md — read section "test/ble/mock_ble_manager_test.dart"; contains fakeAsync test pattern, expectLater placement rule, and stream silence pattern with code-level detail
    - .planning/phases/02-ble-abstraction-mock-layer/02-RESEARCH.md — read Pitfall 2 (expectLater placement), Pitfall 3 (fakeAsync + async/await), and Pattern 6 (expectLater + emitsInOrder)
  </read_first>

  <action>
    Replace the placeholder tests in test/ble/mock_ble_manager_test.dart with real assertions. Keep the same imports block from Task 1. Structure: one `void main()` with one `group('MockBleManager', ...)` containing all tests. Use `final mock = MockBleManager(random: Random(0));` for all tests involving angle or battery values (seeded = deterministic). Call `mock.dispose()` at the end of every test body.

    TEST 1 — MOCK-01 packet rate and angle bounds:
    Use `fakeAsync`. Listen to `mock.statePackets` before calling connect. Call `mock.connect('AA:BB:CC:DD:EE:FF')`. Elapse 300ms to fire the connect delay, call `async.flushFutures()` to resolve the Future. Elapse 1000ms more (10 ticks at 100ms each). Assert `packets.length >= 10`. For each packet in packets: parse it with `StatePacket.parse(packet)` and assert `state.angleX >= -45.0 && state.angleX <= 45.0` and same for `state.angleY`.

    TEST 2 — MOCK-02 battery drift:
    Use `fakeAsync`. Call `mock.connect('AA:BB:CC:DD:EE:FF')`, elapse 300ms, call `async.flushFutures()`. Record battery from the first packet. Elapse 10000ms (100 ticks — one battery drain cycle). Parse the last received packet and assert its battery value is less than the initial battery (started at 85, drained by 1 after 100 ticks → expect 84).

    TEST 3 — MOCK-03 connect() emits connecting then connected:
    This test uses `expectLater` with `emitsInOrder` — the expectation MUST be set up before calling connect(). Pattern: call `expectLater(mock.connectionStatus, emitsInOrder([ConnectionStatus.connecting, ConnectionStatus.connected]))` first, then `await mock.connect('AA:BB:CC:DD:EE:FF')`. This test uses `async` (not fakeAsync) because the assertion waits on Future completion — the 300ms delay in the test runner is acceptable for a single connection test. (Alternatively wrap in fakeAsync: call connect without await, elapse 300ms, flushFutures — either approach is correct; prefer fakeAsync for consistency.)

    TEST 4 — MOCK-04 simulateDisconnect() emits disconnected and silences statePackets:
    Use `fakeAsync`. Connect and elapse until connected (300ms + flushFutures). Start counting packets: `var count = 0; mock.statePackets.listen((_) => count++);`. Elapse 500ms (5 ticks). Record `final countAtDisconnect = count;`. Call `mock.simulateDisconnect()`. Elapse 500ms more. Assert `count == countAtDisconnect` (no new packets after disconnect).

    Additionally assert that `simulateDisconnect()` emitted `ConnectionStatus.disconnected` on `connectionStatus` — set up the `expectLater(mock.connectionStatus, emits(ConnectionStatus.disconnected))` BEFORE calling `simulateDisconnect()`.

    CRITICAL PITFALLS to avoid (from RESEARCH.md):
    - PITFALL-2: `expectLater` must be placed BEFORE the method that emits — broadcast streams don't buffer. If placed after, the test hangs.
    - PITFALL-3: Inside `fakeAsync`, never use `await` on a Future.delayed path without `async.flushFutures()` after the corresponding `elapse()`. Use the sequence: `mock.connect(...)` (no await), `async.elapse(Duration(milliseconds: 300))`, `async.flushFutures()`.
    - PITFALL-4: Subscribe to streams BEFORE calling the method that emits, or the broadcast stream discards the event with no listeners.
  </action>

  <verify>
    <automated>flutter test && flutter analyze</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test` exits with code 0 — all tests pass including Phase 1 ble_protocol_test.dart
    - `flutter analyze` exits with code 0 — zero issues across the entire project
    - `flutter test test/ble/mock_ble_manager_test.dart --verbose` output lists at least 4 passing tests in the MockBleManager group
    - No test uses `await Future.delayed(...)` — all time-based tests use fakeAsync
    - test/ble/mock_ble_manager_test.dart does NOT contain `skip:` on any test
    - MOCK-01: test name contains "packet" or "angle" and passes — angles stay in [-45.0, 45.0]
    - MOCK-02: test name contains "battery" and passes — battery decremented by at least 1 in 10s virtual time
    - MOCK-03: test name contains "connect" and passes — emitsInOrder with connecting then connected
    - MOCK-04: test name contains "simulateDisconnect" and passes — stream goes silent after call
  </acceptance_criteria>

  <done>Full test suite green; flutter analyze clean; all 4 MOCK requirements covered by passing tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → MockBleManager | Test code controls Random seed and calls simulateDisconnect(); this is intentional and confined to test/dev builds |
| Phase 4 debug button → MockBleManager.simulateDisconnect() | Debug escape hatch not on interface; only accessible via concrete MockBleManager reference — no production exposure risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | MockBleManager.simulateDisconnect() | accept | Method not on BleManager interface; only MockBleManager callers (test + debug button in Phase 4) can invoke it; not a production attack surface in WP1 |
| T-02-02 | Denial of Service | StreamController post-dispose tick | mitigate | `if (_packetController.isClosed) return;` guard at top of Timer callback prevents StateError crash from racing dispose() |
| T-02-03 | Information Disclosure | Fixed mock device ID 'AA:BB:CC:DD:EE:FF' | accept | Placeholder value; no real hardware address; WP1 scope only |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced; fake_async is a dart.dev-maintained package already in pubspec.lock — no slopcheck required |
</threat_model>

<verification>
Full phase verification:

1. `flutter test` — all tests pass (Phase 1 + Phase 2)
2. `flutter analyze` — zero issues
3. Grep check: `grep -r "flutter_blue_plus" lib/` returns no matches (architecture constraint from CLAUDE.md)
4. Grep check: `grep "UnimplementedError" lib/ble/mock_ble_manager.dart` returns no matches (all stubs replaced)
5. Grep check: `grep "simulateDisconnect" lib/ble/ble_manager.dart` returns no matches (method must NOT be on the interface)
6. Grep check: `grep "broadcast()" lib/ble/mock_ble_manager.dart` returns 3 matches (one per stream controller)
</verification>

<success_criteria>
Phase 2 is complete when:
- MockBleManager produces animated StatePacket events (10 Hz, angles within ±45°) after connect()
- Battery starts at 85% and decrements 1% per 100 ticks (10 seconds of virtual time)
- connect() transitions through connecting → connected with ~300ms delay, exercising connecting state
- simulateDisconnect() fires a disconnected event and silences statePackets immediately
- flutter test passes (all tests green, Phase 1 tests unbroken)
- flutter analyze reports zero issues
- No flutter_blue_plus import anywhere in lib/
</success_criteria>

<output>
Create `.planning/phases/02-ble-abstraction-mock-layer/02-01-SUMMARY.md` when done.
</output>
```
