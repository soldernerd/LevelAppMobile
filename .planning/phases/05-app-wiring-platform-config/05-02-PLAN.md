---
phase: 05-app-wiring-platform-config
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/providers/device_provider.dart
autonomous: true
requirements: [PERM-02, PERM-03, PERM-04]

must_haves:
  truths:
    - "go_router 17.3.0 is declared in pubspec.yaml dependencies"
    - "permission_handler 12.0.3 is declared in pubspec.yaml dependencies"
    - "device_info_plus is declared in pubspec.yaml dependencies"
    - "blePermissionPermanentlyDeniedProvider (StateProvider<bool>) is exported from device_provider.dart"
  artifacts:
    - path: "pubspec.yaml"
      provides: "go_router, permission_handler, and device_info_plus dependency declarations"
      contains: "go_router"
    - path: "lib/providers/device_provider.dart"
      provides: "blePermissionPermanentlyDeniedProvider"
      exports: ["blePermissionPermanentlyDeniedProvider"]
  key_links:
    - from: "lib/providers/device_provider.dart"
      to: "lib/ui/scan_screen.dart (Plan 05-03)"
      via: "blePermissionPermanentlyDeniedProvider ref.watch"
      pattern: "blePermissionPermanentlyDeniedProvider"
    - from: "lib/providers/device_provider.dart"
      to: "lib/main.dart (Plan 05-03)"
      via: "_container.read(blePermissionPermanentlyDeniedProvider.notifier).state"
      pattern: "blePermissionPermanentlyDeniedProvider"
---

<objective>
Add go_router 17.3.0 and permission_handler 12.0.3 to pubspec.yaml, then add the `blePermissionPermanentlyDeniedProvider` StateProvider to device_provider.dart.

Purpose: Unlocks Plan 05-03 which needs both packages imported in main.dart and ScanScreen, and needs the permission-state provider to exist before it can be read or watched.
Output: pubspec.yaml updated with two new packages; device_provider.dart has the permission state provider appended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-app-wiring-platform-config/05-CONTEXT.md
@.planning/phases/05-app-wiring-platform-config/05-RESEARCH.md
@.planning/phases/05-app-wiring-platform-config/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add go_router and permission_handler via flutter pub add</name>
  <files>pubspec.yaml</files>
  <read_first>pubspec.yaml</read_first>
  <action>
    Run the following three commands sequentially. These are the only package versions sanctioned by CLAUDE.md and verified in RESEARCH.md Package Legitimacy Audit (both from pub.dev verified publishers). `device_info_plus` is needed by Plan 05-03 for Android SDK version detection in the dual-path BLE permission flow.

    Command 1: `flutter pub add go_router:17.3.0`
    Command 2: `flutter pub add permission_handler:12.0.3`
    Command 3: `flutter pub add device_info_plus`

    After all three commands, verify pubspec.yaml contains all three entries under `dependencies:`. Do NOT manually edit pubspec.yaml — let `flutter pub add` handle version constraint formatting and pubspec.lock update.

    If any command fails due to version constraint conflicts, read the error output and check pubspec.yaml for conflicting constraints before re-running. All packages are from verified pub.dev publishers — no slopcheck gate needed.
  </action>
  <verify>
    <automated>grep -c "go_router" pubspec.yaml && grep -c "permission_handler" pubspec.yaml && grep -c "device_info_plus" pubspec.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "go_router" pubspec.yaml` returns at least one match in the dependencies block
    - `grep "permission_handler" pubspec.yaml` returns at least one match in the dependencies block
    - `grep "device_info_plus" pubspec.yaml` returns at least one match in the dependencies block
    - `flutter pub get` exits with code 0 (no resolution conflicts)
    - pubspec.lock updated (contains go_router, permission_handler, and device_info_plus entries)
  </acceptance_criteria>
  <done>go_router 17.3.0, permission_handler 12.0.3, and device_info_plus are resolvable dependencies; flutter pub get succeeds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add blePermissionPermanentlyDeniedProvider to device_provider.dart</name>
  <files>lib/providers/device_provider.dart</files>
  <read_first>lib/providers/device_provider.dart</read_first>
  <behavior>
    - blePermissionPermanentlyDeniedProvider initial state is false
    - Writing true to its notifier makes ref.watch return true
    - Provider is importable from lib/providers/device_provider.dart
  </behavior>
  <action>
    Append a single `StateProvider<bool>` declaration to the end of lib/providers/device_provider.dart, after the existing `instrumentDataProvider` declaration (currently the last provider in the file, ending around line 236).

    The declaration to append:

    ```
    /// Whether BLE permissions are permanently denied on this device.
    ///
    /// Set by main() on cold start (Android only) and updated by ScanScreen
    /// after a permission request resolves to permanentlyDenied.
    /// Watched by ScanScreen to show the inline "Open Settings" message (D-02).
    final blePermissionPermanentlyDeniedProvider = StateProvider<bool>((ref) => false);
    ```

    No new imports are needed — `StateProvider` is already in scope via `flutter_riverpod` (imported on line 3).
    Do NOT modify any existing provider, class, or import in the file.
  </action>
  <verify>
    <automated>flutter test test/providers/</automated>
  </verify>
  <acceptance_criteria>
    - `grep "blePermissionPermanentlyDeniedProvider" lib/providers/device_provider.dart` returns one match
    - The declaration uses `StateProvider&lt;bool&gt;` (not StateNotifierProvider, not Provider)
    - Default value in lambda is `false` (not null, not true)
    - `flutter test test/providers/` exits with code 0 (no provider regressions)
    - No new imports added to device_provider.dart (StateProvider is already available from flutter_riverpod)
  </acceptance_criteria>
  <done>blePermissionPermanentlyDeniedProvider is declared, defaults to false, and all existing provider tests remain green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev registry → pubspec.yaml | Package version pinning; both packages from verified publishers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | go_router 17.3.0, permission_handler 12.0.3 | accept | Both packages from pub.dev verified publishers (flutter.dev, baseflow.com); Package Legitimacy Audit in RESEARCH.md confirms approved status |
| T-05-SC | Tampering | flutter pub add installs | accept | go_router: flutter.dev verified publisher, 3M+/wk downloads. permission_handler: baseflow.com verified publisher, 2.6M+ downloads. Both [Approved] in audit table. |
</threat_model>

<verification>
After both tasks:
- `grep "go_router" pubspec.yaml` shows version entry
- `grep "permission_handler" pubspec.yaml` shows version entry
- `grep "blePermissionPermanentlyDeniedProvider" lib/providers/device_provider.dart` shows one match
- `flutter test test/providers/` exits 0
- `flutter test` exits 0 (full suite green)
</verification>

<success_criteria>
- go_router 17.3.0 in pubspec.yaml dependencies
- permission_handler 12.0.3 in pubspec.yaml dependencies
- blePermissionPermanentlyDeniedProvider declared as StateProvider&lt;bool&gt; defaulting to false
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/05-app-wiring-platform-config/05-02-SUMMARY.md` when done
</output>
