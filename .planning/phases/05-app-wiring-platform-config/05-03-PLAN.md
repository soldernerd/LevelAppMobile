---
phase: 05-app-wiring-platform-config
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - lib/main.dart
  - lib/ui/scan_screen.dart
autonomous: true
requirements: [PERM-02, PERM-03, PERM-04]

must_haves:
  truths:
    - "Cold launch on Android requests BLE permissions showing an app-provided rationale dialog before the system prompt"
    - "If permissions are permanently denied, scan screen shows an inline message with an Open Settings button"
    - "Navigating to /instrument when not connected redirects to /scan"
    - "After connecting, ScanScreen navigates to /instrument via context.go (not Navigator.push)"
    - "ProviderContainer carries bleManagerProvider override; ProviderScope uses parent: _container"
  artifacts:
    - path: "lib/main.dart"
      provides: "Production app entry point with go_router, ProviderContainer, eager permission check"
      exports: ["main"]
      contains: "matchedLocation == '/instrument'"
    - path: "lib/ui/scan_screen.dart"
      provides: "go_router navigation + permission inline UI"
      contains: "context.go('/instrument')"
  key_links:
    - from: "lib/main.dart (_container)"
      to: "lib/providers/device_provider.dart (blePermissionPermanentlyDeniedProvider)"
      via: "_container.read(blePermissionPermanentlyDeniedProvider.notifier).state ="
      pattern: "blePermissionPermanentlyDeniedProvider.notifier"
    - from: "lib/ui/scan_screen.dart"
      to: "go_router /instrument route"
      via: "context.go('/instrument')"
      pattern: "context.go"
---

<objective>
Replace lib/main.dart with the production entry point (ProviderContainer, go_router, RouterNotifier, eager permission check) and update lib/ui/scan_screen.dart with go_router navigation, permission rationale dialog, and inline permanently-denied message.

Purpose: Wires the full app navigation and permission flow. All decisions D-01 through D-07 are implemented here.
Output: A running app that boots on Android, requests BLE permissions with rationale, guards /instrument via go_router, and shows inline denied UI when permanently denied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-app-wiring-platform-config/05-CONTEXT.md
@.planning/phases/05-app-wiring-platform-config/05-RESEARCH.md
@.planning/phases/05-app-wiring-platform-config/05-PATTERNS.md
@.planning/phases/05-app-wiring-platform-config/05-02-SUMMARY.md

<interfaces>
From lib/providers/device_provider.dart (as of Plan 05-02):

```dart
// Existing providers:
final bleManagerProvider = Provider<BleManager>((ref) { ... });
final connectionNotifierProvider = NotifierProvider<ConnectionNotifier, ConnectionStatus>(ConnectionNotifier.new);
final scanResultsProvider = Provider<List<ScannedDevice>>((ref) { ... });
final instrumentDataProvider = StreamProvider<DeviceState?>((ref) { ... });

// Added in Plan 05-02:
final blePermissionPermanentlyDeniedProvider = StateProvider<bool>((ref) => false);

// Models in lib/models/device_state.dart:
enum ConnectionStatus { idle, scanning, connecting, connected, reconnecting, disconnecting, disconnected, error }
```

From lib/ble/mock_ble_manager.dart:
```dart
class MockBleManager implements BleManager { ... }
// Constructor: MockBleManager() — no required arguments
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace lib/main.dart with production entry point (D-05, D-06, D-07)</name>
  <files>lib/main.dart</files>
  <read_first>lib/main.dart, lib/providers/device_provider.dart, lib/ble/mock_ble_manager.dart</read_first>
  <action>
    Completely replace lib/main.dart. The current file is Phase 4 temporary code (see comment on line 1); replace its entire contents.

    The new file must contain, in this order:

    1. Imports: `dart:io` (for Platform.isAndroid), `package:flutter/material.dart`, `package:flutter_riverpod/flutter_riverpod.dart`, `package:go_router/go_router.dart`, `package:permission_handler/permission_handler.dart`, `package:inclinometer/ble/mock_ble_manager.dart`, `package:inclinometer/models/device_state.dart`, `package:inclinometer/providers/device_provider.dart`, `package:inclinometer/ui/instrument_screen.dart`, `package:inclinometer/ui/scan_screen.dart`.

    2. Top-level ProviderContainer (D-07): `final _container = ProviderContainer(overrides: [bleManagerProvider.overrideWithValue(MockBleManager())]);`
       This is the WP2 swap point — comment it as such.

    3. GoRouter instance (D-03, D-04, D-06):
       - Do NOT include `refreshListenable` — omit it entirely (D-04 constraint: adding refreshListenable wired to connectionNotifierProvider would cause the redirect to fire on every disconnect event, ejecting the user from /instrument while already on the screen. After connecting, ScanScreen calls `context.go('/instrument')` explicitly in Task 2, so no self-triggered redirect is needed.)
       - `initialLocation: '/scan'`
       - redirect callback: checks `state.matchedLocation == '/instrument'` AND `_container.read(connectionNotifierProvider) != ConnectionStatus.connected`; returns `'/scan'` when both true, returns `null` for all other cases (including when already at /scan — prevents redirect loop per RESEARCH.md Pitfall 1)
       - routes: GoRoute(path: '/scan', builder: ScanScreen), GoRoute(path: '/instrument', builder: InstrumentScreen)
       - D-04 is satisfied because: (a) the redirect fires only on navigation attempts, never on live state changes without `refreshListenable`; (b) post-connect navigation to /instrument is handled by `context.go('/instrument')` in ScanScreen Task 2, not by the router self-evaluating.

    6. main() async function:
       - First line: `WidgetsFlutterBinding.ensureInitialized()` (RESEARCH.md Pitfall 2 — required before permission_handler platform channel calls)
       - Android-only block: check `await Permission.bluetoothScan.isPermanentlyDenied || await Permission.bluetoothConnect.isPermanentlyDenied`; write result to `_container.read(blePermissionPermanentlyDeniedProvider.notifier).state` (D-01 cold-start check)
       - runApp with `ProviderScope(parent: _container, child: MaterialApp.router(...))` — use `parent: _container` NOT `overrides:` (RESEARCH.md Pitfall 3; overrides are already on the container)
       - MaterialApp.router: `routerConfig: _router`, `theme: ThemeData.dark()`, `debugShowCheckedModeBanner: false`, `title: 'Inclinometer'` (D-05)

    Do NOT use `ProviderScope(overrides: [...])` — all overrides must live in `_container`.
    Do NOT use `state.subloc` — use `state.matchedLocation` (go_router 6+ API, RESEARCH.md State of the Art).
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "WidgetsFlutterBinding.ensureInitialized" lib/main.dart` returns one match
    - `grep "ProviderContainer" lib/main.dart` returns at least one match (the _container declaration)
    - `grep "parent: _container" lib/main.dart` returns one match (ProviderScope uses parent:, not overrides:)
    - `grep "refreshListenable" lib/main.dart` returns zero matches (must NOT be present — see D-04)
    - `grep "_RouterNotifier\|RouterNotifier" lib/main.dart` returns zero matches (no longer needed without refreshListenable)
    - `grep "matchedLocation.*'/instrument'" lib/main.dart` returns one match (the guard)
    - `grep "'/scan'" lib/main.dart` returns the redirect return value (return '/scan' inside redirect callback)
    - `grep "flutter.minSdkVersion\|flutter.compileSdkVersion" lib/main.dart` returns zero matches (those belong in build.gradle.kts only)
    - `flutter test` exits with code 0
    - `flutter analyze lib/main.dart` exits with code 0 (no static analysis errors)
  </acceptance_criteria>
  <done>lib/main.dart is the production entry point: ProviderContainer with MockBleManager override, RouterNotifier bridge, GoRouter with /scan + /instrument routes and /instrument guard, eager Android permission check before runApp, ThemeData.dark()</done>
</task>

<task type="auto">
  <name>Task 2: Update ScanScreen for go_router navigation, permission rationale, and inline denied UI (D-01, D-02)</name>
  <files>lib/ui/scan_screen.dart</files>
  <read_first>lib/ui/scan_screen.dart, lib/providers/device_provider.dart</read_first>
  <action>
    Make three targeted edits to lib/ui/scan_screen.dart. Do NOT rewrite the file; only change the specific sections described.

    Edit 1 — Imports: Remove `import 'package:inclinometer/ui/instrument_screen.dart';` (line 5 — no longer needed for navigation). Add two new imports: `import 'package:go_router/go_router.dart';` and `import 'package:permission_handler/permission_handler.dart';`

    Edit 2 — Navigation: Replace the existing ref.listen block (lines 26–33, currently using Navigator.of(context).push(MaterialPageRoute(builder: (_) => const InstrumentScreen()))) with go_router navigation per RESEARCH.md Pitfall 4:
    ```
    ref.listen(connectionNotifierProvider, (prev, next) {
      if (next == ConnectionStatus.connected && prev != ConnectionStatus.connected) {
        context.go('/instrument');
      }
    });
    ```
    Keep the guard condition (`next == connected && prev != connected`) identical — only the navigation call changes.

    Edit 3 — Permission state and UI: In the build() method, add a watch on `blePermissionPermanentlyDeniedProvider` immediately after the existing `final devices = ref.watch(...)` line:
    ```
    final permanentlyDenied = ref.watch(blePermissionPermanentlyDeniedProvider);
    ```

    Then add permission-related behavior:

    a) Rationale dialog (D-01): Add a `WidgetsBinding.instance.addPostFrameCallback` call in build() (before the return Scaffold) that triggers `_requestBlePermissionsIfNeeded(context, ref)` on first render when on Android. Use a `_permissionRequested` instance variable (or check permission status inline) to ensure the dialog shows only once per session, not on every rebuild. Because ScanScreen is a ConsumerWidget (stateless), use a top-level bool flag `_blePermissionsRequested` at file scope, initialized to false, set to true after the first callback fires.

    b) Inline denied banner (D-02): Wrap the Scaffold body with a Column that conditionally shows the denied banner at the top when `permanentlyDenied == true`. The banner is a Container with a Row containing an Expanded Text ("Bluetooth permission is permanently denied. Open Settings to grant access.") and a TextButton ("Open Settings") that calls `openAppSettings()`. Insert this banner as the first child of a Column wrapping the existing `_buildBody(...)` call:
    ```
    body: Column(children: [
      if (permanentlyDenied) _buildPermissionDeniedBanner(),
      Expanded(child: _buildBody(context, ref, status, devices)),
    ]),
    ```

    c) Add a helper method `Future<void> _requestBlePermissionsIfNeeded(BuildContext context, WidgetRef ref) async` as a file-level function (not a class method since ScanScreen is a ConsumerWidget). This function:
       - Checks if Platform.isAndroid; if not, returns immediately.
       - Determines the Android SDK version using `(await DeviceInfoPlugin().androidInfo).version.sdkInt`. Requires adding `device_info_plus` to pubspec.yaml via `flutter pub add device_info_plus` before use.
       - Selects the permission set based on SDK version:
         - API >= 31: `permissions = [Permission.bluetoothScan, Permission.bluetoothConnect]`
         - API < 31: `permissions = [Permission.bluetooth, Permission.locationWhenInUse]`
       - Checks if all selected permissions are already granted; if so, returns immediately.
       - Shows a rationale AlertDialog with title "Bluetooth Permission" and body "This app needs Bluetooth to scan for and connect to your inclinometer instrument." with a single "Continue" action.
       - After dialog dismissed, calls `permissions.request()`.
       - Checks if any returned status `isPermanentlyDenied`; if so, writes `ref.read(blePermissionPermanentlyDeniedProvider.notifier).state = true`.

    d) Add a helper Widget method `Widget _buildPermissionDeniedBanner()` at file scope that returns the banner Container described in (b) above. Background color: `Color(0xFF311B1B)`. Text style: fontSize 13, color Colors.white70.

    The scan screen body layout, FAB, AppBar, chip, device list, and all other existing behaviour remain unchanged.
  </action>
  <verify>
    <automated>flutter test test/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `grep "context.go('/instrument')" lib/ui/scan_screen.dart` returns one match
    - `grep "Navigator.of(context).push" lib/ui/scan_screen.dart` returns zero matches (old navigation removed)
    - `grep "instrument_screen.dart" lib/ui/scan_screen.dart` returns zero matches (import removed)
    - `grep "blePermissionPermanentlyDeniedProvider" lib/ui/scan_screen.dart` returns at least one match (ref.watch)
    - `grep "openAppSettings" lib/ui/scan_screen.dart` returns one match (in denied banner)
    - `grep "go_router" lib/ui/scan_screen.dart` returns one match (import)
    - `grep "sdkInt" lib/ui/scan_screen.dart` returns one match (dual-path SDK version branch in _requestBlePermissionsIfNeeded)
    - `flutter test test/ui/` exits with code 0 (existing widget tests remain green)
    - `flutter analyze lib/ui/scan_screen.dart` exits with code 0
  </acceptance_criteria>
  <done>ScanScreen navigates via context.go('/instrument'), shows rationale dialog before permission request on Android, and shows inline "Open Settings" banner when blePermissionPermanentlyDeniedProvider is true</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android OS → Flutter app | Runtime permission grant/deny enforced by Android; app reads result via permission_handler |
| go_router redirect → routes | Route guard prevents unauthenticated navigation to /instrument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | go_router /instrument route | mitigate | redirect callback returns '/scan' for any state != connected; matchedLocation check prevents false positives on /scan (RESEARCH.md Pitfall 1) |
| T-05-04 | Spoofing | Permission rationale dialog | accept | App shows its own rationale before system prompt; system permission dialog is OS-enforced and cannot be spoofed |
| T-05-05 | Information Disclosure | Stale data post-disconnect | accept | Already mitigated in Phase 3 (null sentinel from ConnectionNotifier); D-04 ensures app stays on instrument screen intentionally — user sees stale indicator from Phase 4 InstrumentScreen implementation |
| T-05-SC | Tampering | go_router, permission_handler packages | accept | Both packages added in Plan 05-02 with verified publisher audit; no new installs in this plan |
</threat_model>

<verification>
After both tasks:
- `flutter test` exits 0 (full test suite green — providers, models, widget tests)
- `flutter analyze lib/` exits 0 (no static analysis errors)
- `grep "WidgetsFlutterBinding.ensureInitialized" lib/main.dart` shows one match
- `grep "parent: _container" lib/main.dart` shows one match
- `grep "matchedLocation.*instrument" lib/main.dart` shows one match
- `grep "context.go" lib/ui/scan_screen.dart` shows one match
- `grep "Navigator.of(context).push" lib/ui/scan_screen.dart` shows zero matches
- Manual verification on device/emulator: cold launch → rationale dialog → system BLE prompt → scan screen (see Phase 5 success criteria 1–2 in ROADMAP.md)
</verification>

<success_criteria>
- PERM-02: App requests BLE permissions via permission_handler (bluetoothScan + bluetoothConnect)
- PERM-03: App shows its own rationale AlertDialog before calling .request()
- PERM-04: Permanently denied state shows inline banner with openAppSettings() button on scan screen
- go_router /instrument guard redirects to /scan when not connected (D-03)
- Guard is navigation-only — does not eject user from /instrument on disconnect (D-04)
- ProviderContainer carries MockBleManager override; ProviderScope uses parent: _container (D-07)
- ThemeData.dark() only, no light/adaptive theme (D-05)
- All existing flutter tests pass
</success_criteria>

<output>
Create `.planning/phases/05-app-wiring-platform-config/05-03-SUMMARY.md` when done
</output>
