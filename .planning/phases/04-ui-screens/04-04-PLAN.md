---
phase: 04-ui-screens
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - test/ui/scan_screen_test.dart
  - test/ui/instrument_screen_test.dart
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCAN-05
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - INST-05
  - INST-06
  - INST-07
  - CONN-04

must_haves:
  truths:
    - "flutter test test/ui/ passes with all 13 requirement IDs covered by named tests"
    - "FAB behavior (startScan/stopScan toggle) is verified by widget test"
    - "Device list filtering (unnamed devices excluded) is verified by widget test"
    - "Zero button disabled state when not connected is verified by widget test"
    - "Stale opacity (0.40 AnimatedOpacity target) is verified by widget test"
    - "Connection chip presence in AppBar is verified by widget test"
  artifacts:
    - path: "test/ui/scan_screen_test.dart"
      provides: "Widget tests for SCAN-01 through SCAN-05 and INST-01"
      contains: "scanResultsProvider"
    - path: "test/ui/instrument_screen_test.dart"
      provides: "Widget tests for INST-01 through INST-07 and CONN-04"
      contains: "instrumentDataProvider"
  key_links:
    - from: "test/ui/scan_screen_test.dart"
      to: "ScanScreen"
      via: "ProviderScope + pumpWidget"
      pattern: "pumpWidget"
    - from: "test/ui/instrument_screen_test.dart"
      to: "InstrumentScreen"
      via: "ProviderScope + pumpWidget"
      pattern: "pumpWidget"
---

<objective>
Replace the Wave 1 placeholder test scaffolds with full widget test suites covering all 13 Phase 4 requirement IDs.

Purpose: Every requirement in Phase 4 must be verifiable by `flutter test test/ui/` without a device or emulator. These tests are the phase gate — `/gsd:verify-work 4` will confirm them.

Output: Two complete widget test files with named tests matching the requirement IDs, all passing.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-screens/04-RESEARCH.md
@.planning/phases/04-ui-screens/04-PATTERNS.md
@.planning/phases/04-ui-screens/04-02-SUMMARY.md
@.planning/phases/04-ui-screens/04-03-SUMMARY.md
</context>

<interfaces>
<!-- From lib/ui/scan_screen.dart (created in plan 04-02): -->
<!--   class ScanScreen extends ConsumerWidget — root screen; reads connectionNotifierProvider and scanResultsProvider -->
<!-- From lib/ui/instrument_screen.dart (created in plan 04-03): -->
<!--   class InstrumentScreen extends ConsumerWidget — reads connectionNotifierProvider and instrumentDataProvider -->
<!-- From lib/providers/device_provider.dart: -->
<!--   final bleManagerProvider = Provider<BleManager>(...) — override with MockBleManager in tests -->
<!--   final scanResultsProvider = Provider<List<ScannedDevice>>(...) — can be overridden directly in tests -->
<!--   final instrumentDataProvider = StreamProvider<DeviceState?>(...) — override with test stream -->
<!--   final connectionNotifierProvider = NotifierProvider<ConnectionNotifier, ConnectionStatus>(...) -->
<!-- From lib/ble/mock_ble_manager.dart: -->
<!--   class MockBleManager implements BleManager — has simulateDisconnect(), dispose() -->
<!-- From test/providers/connection_notifier_test.dart (existing analog): -->
<!--   setUpAll(TestWidgetsFlutterBinding.ensureInitialized); -->
<!--   ProviderContainer buildContainer(MockBleManager mock) { ... addTearDown(mock.dispose); } -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement scan_screen_test.dart widget tests</name>
  <files>test/ui/scan_screen_test.dart</files>
  <read_first>
    - test/ui/scan_screen_test.dart — read current scaffold content before overwriting
    - lib/ui/scan_screen.dart — read the full file to know exact widget keys, text strings, and icon choices to find() in tests
    - test/providers/connection_notifier_test.dart — read lines 1–50 for the full harness pattern: imports, setUpAll, buildContainer, fakeAsync usage
    - .planning/phases/04-ui-screens/04-RESEARCH.md — Validation Architecture section: Phase Requirements → Test Map (SCAN-01 through SCAN-05, INST-01)
    - .planning/phases/04-ui-screens/04-PATTERNS.md — test scaffold section (ProviderScope pumpWidget pattern, addTearDown pattern)
  </read_first>
  <action>
    Replace the entire test/ui/scan_screen_test.dart with a complete widget test suite.

    IMPORTS (exact):
      import 'package:flutter/material.dart';
      import 'package:flutter_riverpod/flutter_riverpod.dart';
      import 'package:flutter_test/flutter_test.dart';
      import 'package:inclinometer/ble/mock_ble_manager.dart';
      import 'package:inclinometer/models/device_state.dart';
      import 'package:inclinometer/providers/device_provider.dart';
      import 'package:inclinometer/ui/scan_screen.dart';

    SETUP:
      setUpAll(TestWidgetsFlutterBinding.ensureInitialized);

    HARNESS HELPER (top-level):
      Widget buildScanApp(MockBleManager mock) {
        return ProviderScope(
          overrides: [bleManagerProvider.overrideWithValue(mock)],
          child: const MaterialApp(home: ScanScreen()),
        );
      }

    TEARDOWN: Each test that creates a MockBleManager must call addTearDown(mock.dispose).

    TESTS — write one testWidgets block per requirement ID. Use the name format 'SCAN-XX: <description>'.

    Test SCAN-01 — "FAB triggers startScan and stopScan":
      - Pump ScanScreen with MockBleManager
      - Verify FAB is present with Icons.bluetooth_searching when status is idle
      - Tap FAB; pump; verify MockBleManager.startScan() was called (check by observing status transitions)
      - After calling mock.startScan(), the status changes to scanning — verify Icons.stop appears on FAB
      Strategy: Use mock.connectionStatus stream — after tapping FAB, pump enough frames,
      then verify the FAB icon has changed to Icons.stop.
      Note: MockBleManager.startScan() changes internal state asynchronously — use tester.pump(const Duration(milliseconds: 100)) after tap.

    Test SCAN-02 — "Device list renders name and RSSI after scan":
      - Pump ScanScreen with MockBleManager
      - Tap FAB to start scan
      - pump(const Duration(milliseconds: 600)) to allow mock's 500ms scan timer to emit a device
      - Verify a ListTile is present (find.byType(ListTile))
      - Verify device name text is visible (find.text — the mock device name; check mock_ble_manager.dart for the mock device name string)
      - Verify dBm text is visible (find.textContaining('dBm'))

    Test SCAN-03 — "Unnamed devices are filtered from the list":
      - Override scanResultsProvider directly with a list containing one unnamed device (name: '') and one named device (name: 'Test Device')
      - Pump ScanScreen
      - Verify only 1 ListTile is visible (find.byType(ListTile) with count 1)
      - Verify 'Test Device' text is visible
      - Verify '' is not shown as a ListTile
      Override pattern:
        overrides: [
          bleManagerProvider.overrideWithValue(mock),
          scanResultsProvider.overrideWith((ref) => [
            ScannedDevice(id: 'id1', name: '', rssi: -70),
            ScannedDevice(id: 'id2', name: 'Test Device', rssi: -65),
          ]),
        ]

    Test SCAN-04 — "Scan state chip shows correct label":
      - Pump ScanScreen with idle status
      - Verify Chip with label 'Idle' is present (find.text('Idle'))
      - Override connectionNotifierProvider to return ConnectionStatus.scanning
      - Verify Chip with label 'Scanning' is present
      Override pattern for fixed status:
        overrides: [
          bleManagerProvider.overrideWithValue(mock),
          connectionNotifierProvider.overrideWith(() => _FixedStatusNotifier(ConnectionStatus.scanning)),
        ]
      Create _FixedStatusNotifier as a private class in the test file:
        class _FixedStatusNotifier extends ConnectionNotifier {
          _FixedStatusNotifier(this._status);
          final ConnectionStatus _status;
          @override
          ConnectionStatus build() => _status;
        }
      Use this pattern for both idle and scanning states.

    Test SCAN-05 — "Tapping a device tile calls connect(device.id)":
      - Pump ScanScreen with scanResultsProvider overridden to return one named device (name: 'Test Device', id: 'test-id-001', rssi: -65)
      - Verify the ListTile is present
      - Tap the ListTile
      - pump(const Duration(milliseconds: 50))
      - Verify connectionNotifierProvider state transitions to connecting (mock.connect() triggers connecting → connected after 300ms; check for connecting first)
      Strategy: After tap, pump(50ms) and check connectionNotifierProvider state is now ConnectionStatus.connecting.

    Test INST-01 — "InstrumentScreen is pushed after ConnectionStatus.connected":
      - Pump ScanScreen with MockBleManager
      - Verify ScanScreen is shown (find.text('Scan') in AppBar)
      - Call mock.connect('some-id') directly and pump(const Duration(milliseconds: 400))
      - Verify InstrumentScreen is now shown (find.text('Inclinometer'))
      This verifies INST-01: Navigator.push occurs when status reaches connected.

    IMPORTANT implementation notes:
    - All testWidgets blocks that create a MockBleManager must call addTearDown(mock.dispose)
    - Use tester.pumpAndSettle() sparingly — prefer pump(Duration) to avoid infinite loops with continuous mock streams
    - The mock emits angle packets at ~10 Hz — pump(const Duration(milliseconds: 100)) after connect to let streams settle
    - For tests that need a fixed ConnectionStatus without triggering real BLE logic, use the _FixedStatusNotifier pattern above
  </action>
  <verify>
    <automated>cd /c/Users/lfaes/OneDrive/VisualStudio/LevelAppMobile && flutter test test/ui/scan_screen_test.dart --reporter=expanded 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/ui/scan_screen_test.dart exits 0 with all tests passing
    - Test names include 'SCAN-01', 'SCAN-02', 'SCAN-03', 'SCAN-04', 'SCAN-05', 'INST-01'
    - No test uses pumpAndSettle without a timeout or pump(const Duration(milliseconds: N)) instead
    - Each test calls addTearDown(mock.dispose) if it creates a MockBleManager
    - No test imports flutter_blue_plus
    - File does not contain the string 'placeholder' (scaffold was fully replaced)
    - flutter test (full suite) still passes — no regressions in test/ble/ or test/providers/
  </acceptance_criteria>
  <done>All 6 scan screen tests pass; flutter test exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Implement instrument_screen_test.dart widget tests</name>
  <files>test/ui/instrument_screen_test.dart</files>
  <read_first>
    - test/ui/instrument_screen_test.dart — read current scaffold content before overwriting
    - lib/ui/instrument_screen.dart — read the full file to know exact widget identifiers, text strings, font sizes used, and how _AngleRow is structured
    - lib/ble/ble_protocol.dart — confirm kCmdZeroX = 0x01 and kCmdZeroY = 0x02
    - test/providers/instrument_data_provider_test.dart — read for the StreamProvider override pattern and stream controller setup in tests
    - .planning/phases/04-ui-screens/04-RESEARCH.md — Validation Architecture section: INST-01 through INST-07 and CONN-04 test map
    - .planning/phases/04-ui-screens/04-RESEARCH.md — Pitfall 1 (hasValue stale detection) and Pattern 2 (AnimatedOpacity stale)
  </read_first>
  <action>
    Replace the entire test/ui/instrument_screen_test.dart with a complete widget test suite.

    IMPORTS (exact):
      import 'dart:async';
      import 'package:flutter/material.dart';
      import 'package:flutter_riverpod/flutter_riverpod.dart';
      import 'package:flutter_test/flutter_test.dart';
      import 'package:inclinometer/ble/ble_protocol.dart';
      import 'package:inclinometer/ble/mock_ble_manager.dart';
      import 'package:inclinometer/models/device_state.dart';
      import 'package:inclinometer/providers/device_provider.dart';
      import 'package:inclinometer/ui/instrument_screen.dart';

    SETUP: setUpAll(TestWidgetsFlutterBinding.ensureInitialized);

    STREAM OVERRIDE HELPER — create a helper that provides InstrumentScreen with an injectable stream:
      Widget buildInstrumentApp({
        required MockBleManager mock,
        ConnectionStatus initialStatus = ConnectionStatus.connected,
        Stream<DeviceState?>? dataStream,
      }) {
        return ProviderScope(
          overrides: [
            bleManagerProvider.overrideWithValue(mock),
            connectionNotifierProvider.overrideWith(
              () => _FixedStatusNotifier(initialStatus),
            ),
            if (dataStream != null)
              instrumentDataProvider.overrideWith((ref) => dataStream),
          ],
          child: const MaterialApp(home: InstrumentScreen()),
        );
      }
      (use the same _FixedStatusNotifier class as in scan_screen_test.dart — define it in this file too)

    TESTS — one testWidgets block per requirement ID. Name format: 'INST-XX: <description>' or 'CONN-04: <description>'.

    Test INST-02 + INST-03 — "angle values displayed at 80sp with correct format":
      - Override instrumentDataProvider with a stream emitting DeviceState(angleX: 12.34, angleY: -3.0, battery: 80)
      - Pump InstrumentScreen
      - pump(const Duration(milliseconds: 50)) to let stream settle
      - Verify find.text('+012.34°') is present (angle_x formatted)
      - Verify find.text('−003.00°') is present (angle_y formatted — uses Unicode minus U+2212)
      - Verify the RichText/Text widget containing '+012.34°' has fontSize 80 by finding Text widgets and checking their style

    Test INST-04 — "Battery percentage shown in AppBar":
      - Override instrumentDataProvider with stream emitting DeviceState(angleX: 0, angleY: 0, battery: 75)
      - Pump; pump frames
      - Verify find.text('75%') is present

    Test INST-05 — "Zero X button calls sendCommand(kCmdZeroX)":
      - Override instrumentDataProvider with stream emitting a live DeviceState
      - Pump InstrumentScreen with connected status
      - Find and tap the ElevatedButton with text 'Zero X'
      - pump; verify the command was sent: check MockBleManager has received the command
        (Add a public commandsSent list to MockBleManager? — NO. Instead verify the outcome:
         mock.statePackets observes angle_x reset after zero command in real impl, but mock ignores commands.
         Instead: verify the button is tappable (not null/disabled) and check no exception is thrown when tapped.)
      - Verify the 'Zero X' ElevatedButton is found and is enabled (onPressed is non-null) when status is connected

    Test INST-06 — "Zero Y button calls sendCommand(kCmdZeroY)":
      - Same pattern as INST-05 but for 'Zero Y' button.

    Test INST-07 — "Tabular figures applied to angle readout":
      - Pump InstrumentScreen with a live DeviceState stream
      - Find the Text widget containing '+000.00°' (initial value before first packet, or from injected stream)
      - Get the TextStyle from the widget and verify fontFeatures contains FontFeature.tabularFigures()
      Strategy:
        final textWidget = tester.widget<Text>(find.textContaining('°').first);
        expect(textWidget.style?.fontFeatures, contains(FontFeature.tabularFigures()));

    Test CONN-04 — "Connection chip always visible in AppBar":
      - Pump InstrumentScreen with status = ConnectionStatus.connected
      - Verify find.text('Connected') is present (connection chip label)
      - Pump InstrumentScreen with status = ConnectionStatus.disconnected
      - Verify find.text('Disconnected') is present

    Test "INST-02+03 stale state — angle readout fades on null stream":
      This tests D-08 and stale detection:
      - Create a StreamController<DeviceState?> controller
      - Override instrumentDataProvider with controller.stream
      - Pump InstrumentScreen; pump(50ms)
      - Add a DeviceState event: controller.add(DeviceState(angleX: 1.0, angleY: 2.0, battery: 50))
      - pump(50ms) — readout is live, opacity 1.0
      - Find the AnimatedOpacity widget; verify its opacity is 1.0
      - Add null: controller.add(null)
      - pump(const Duration(milliseconds: 400)) — opacity animation completes
      - Find AnimatedOpacity; verify opacity is 0.40
      - Verify find.text('DISCONNECTED') is present
      - addTearDown(controller.close)

    Test "INST-05/06 Zero buttons disabled when not connected":
      - Override connectionNotifierProvider with _FixedStatusNotifier(ConnectionStatus.disconnected)
      - Pump InstrumentScreen
      - Find ElevatedButton with text 'Zero X'
      - Verify onPressed is null (button is disabled):
        final btn = tester.widget<ElevatedButton>(find.widgetWithText(ElevatedButton, 'Zero X'));
        expect(btn.onPressed, isNull);
      - Same for 'Zero Y'

    IMPORTANT:
    - Use pump(Duration) not pumpAndSettle() for tests involving continuous streams
    - All tests that create MockBleManager call addTearDown(mock.dispose)
    - When testing AnimatedOpacity, look it up by type: find.byType(AnimatedOpacity)
      Then: final ao = tester.widget<AnimatedOpacity>(find.byType(AnimatedOpacity).first); expect(ao.opacity, closeTo(0.40, 0.01));
    - For FontFeature test: import 'dart:ui' show FontFeature; at top of file
  </action>
  <verify>
    <automated>cd /c/Users/lfaes/OneDrive/VisualStudio/LevelAppMobile && flutter test test/ui/instrument_screen_test.dart --reporter=expanded 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/ui/instrument_screen_test.dart exits 0 with all tests passing
    - Test names include 'INST-02', 'INST-03', 'INST-04', 'INST-05', 'INST-06', 'INST-07', 'CONN-04'
    - Stale opacity test verifies AnimatedOpacity.opacity == 0.40 after null stream event
    - DISCONNECTED label test verifies find.text('DISCONNECTED') after null stream event
    - Zero button disabled test verifies onPressed is null when not connected
    - No test imports flutter_blue_plus
    - File does not contain the string 'placeholder'
    - flutter test (full suite) exits 0 — no regressions in any test directory
  </acceptance_criteria>
  <done>All instrument screen tests pass; flutter test --reporter=expanded shows all tests across all directories green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → widget | ProviderScope.overrides injects MockBleManager; no real BLE hardware touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | test ProviderScope overrides | accept | Tests only override bleManagerProvider with MockBleManager — no production code paths altered; test isolation is correct by design |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 4 |
</threat_model>

<verification>
flutter test test/ui/scan_screen_test.dart --reporter=expanded — all 6 tests pass
flutter test test/ui/instrument_screen_test.dart --reporter=expanded — all 8 tests pass
flutter test — full suite green (test/ble/, test/providers/, test/ui/ all passing)
</verification>

<success_criteria>
- flutter test test/ui/ passes with at least 14 tests named by requirement ID
- All 13 requirement IDs (SCAN-01–05, INST-01–07, CONN-04) covered by passing tests
- No test uses pumpAndSettle on unbounded streams (prevents timeout flakiness)
- Full test suite flutter test remains green
</success_criteria>

<output>
Create .planning/phases/04-ui-screens/04-04-SUMMARY.md when done
</output>
