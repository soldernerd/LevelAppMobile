---
phase: 05-app-wiring-platform-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - ios/Runner/Info.plist
autonomous: true
requirements: [PERM-01, PERM-05, BUILD-01, BUILD-02]

must_haves:
  truths:
    - "build.gradle.kts declares minSdk = 24 and compileSdk = 35 as hardcoded integers (not flutter.* delegates)"
    - "AndroidManifest.xml declares BLUETOOTH_SCAN, BLUETOOTH_CONNECT, and ACCESS_FINE_LOCATION before the <application> block"
    - "ios/Runner/Info.plist contains NSBluetoothAlwaysUsageDescription key with a non-empty string"
  artifacts:
    - path: "android/app/build.gradle.kts"
      provides: "SDK version declarations"
      contains: "minSdk = 24"
    - path: "android/app/build.gradle.kts"
      provides: "SDK version declarations"
      contains: "compileSdk = 35"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Android BLE permission declarations"
      contains: "BLUETOOTH_SCAN"
    - path: "ios/Runner/Info.plist"
      provides: "iOS Bluetooth usage description"
      contains: "NSBluetoothAlwaysUsageDescription"
  key_links:
    - from: "build.gradle.kts"
      to: "android SDK toolchain"
      via: "compileSdk integer value"
      pattern: "compileSdk = 35"
    - from: "AndroidManifest.xml"
      to: "Android runtime permission system"
      via: "uses-permission declarations"
      pattern: "BLUETOOTH_SCAN.*neverForLocation"
---

<objective>
Set the Android build SDK versions to their required hardcoded integers and declare all required platform permissions in AndroidManifest.xml and Info.plist.

Purpose: Satisfies BUILD-01, BUILD-02, PERM-01, PERM-05 before any Dart wiring begins. These are pure config file edits with no Dart dependency.
Output: build.gradle.kts with minSdk=24/compileSdk=35, AndroidManifest.xml with BLE + location permissions, Info.plist with Bluetooth usage description.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-app-wiring-platform-config/05-CONTEXT.md
@.planning/phases/05-app-wiring-platform-config/05-RESEARCH.md
@.planning/phases/05-app-wiring-platform-config/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden build.gradle.kts SDK versions (BUILD-01, BUILD-02, D-08)</name>
  <files>android/app/build.gradle.kts</files>
  <read_first>android/app/build.gradle.kts</read_first>
  <action>
    Replace the two delegate references with hardcoded integers. These are the only two lines that change; leave every other line untouched.

    Line 9: replace `compileSdk = flutter.compileSdkVersion` with `compileSdk = 35`
    Line 22: replace `minSdk = flutter.minSdkVersion` with `minSdk = 24`

    Do NOT touch: ndkVersion (line 10), targetSdk = flutter.targetSdkVersion (line 23), versionCode, versionName, namespace, applicationId, buildTypes, compileOptions, or the kotlin/flutter blocks.

    Kotlin DSL syntax rule (D-08, RESEARCH.md Pitfall 6): use `= 24` (assignment with equals sign), not `24` bare. The file already uses Kotlin DSL — the existing `namespace = "..."` pattern confirms this.
  </action>
  <verify>
    <automated>grep -c "minSdk = 24" android/app/build.gradle.kts && grep -c "compileSdk = 35" android/app/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "minSdk = 24" android/app/build.gradle.kts` returns exactly one match
    - `grep "compileSdk = 35" android/app/build.gradle.kts` returns exactly one match
    - `grep "flutter.minSdkVersion" android/app/build.gradle.kts` returns zero matches
    - `grep "flutter.compileSdkVersion" android/app/build.gradle.kts` returns zero matches
    - `grep "targetSdk = flutter.targetSdkVersion" android/app/build.gradle.kts` still returns one match (not touched)
  </acceptance_criteria>
  <done>build.gradle.kts declares minSdk=24 and compileSdk=35 as integer literals; no flutter.* SDK delegates remain for min or compile targets</done>
</task>

<task type="auto">
  <name>Task 2: Declare BLE permissions in AndroidManifest.xml and iOS Info.plist (PERM-01, PERM-05)</name>
  <files>android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</files>
  <read_first>android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</read_first>
  <action>
    AndroidManifest.xml: Insert five uses-permission elements immediately after the opening `&lt;manifest xmlns:android="..."&gt;` tag and before the `&lt;application` block (currently line 2). The `&lt;application&gt;` block and `&lt;queries&gt;` block remain completely untouched.

    Insert these elements in order:
    1. `&lt;uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" /&gt;`
    2. `&lt;uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /&gt;`
    3. `&lt;uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /&gt;`
    4. `&lt;uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" /&gt;`
    5. `&lt;uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" /&gt;`

    The `neverForLocation` flag on BLUETOOTH_SCAN is mandatory — it declares the app does not derive physical location from BLE scans. Items 4 and 5 (maxSdkVersion="30") are legacy backward-compat entries for Android API 24–30.

    ios/Runner/Info.plist: Insert the NSBluetoothAlwaysUsageDescription key/value pair inside the root &lt;dict&gt; element, immediately before the closing `&lt;/dict&gt;` tag (currently line 69). Do NOT add NSBluetoothPeripheralUsageDescription — that key is deprecated since iOS 13.

    Insert:
    `&lt;key&gt;NSBluetoothAlwaysUsageDescription&lt;/key&gt;`
    `&lt;string&gt;This app needs Bluetooth access to connect to your inclinometer instrument.&lt;/string&gt;`
  </action>
  <verify>
    <automated>grep -c "BLUETOOTH_SCAN" android/app/src/main/AndroidManifest.xml && grep -c "BLUETOOTH_CONNECT" android/app/src/main/AndroidManifest.xml && grep -c "ACCESS_FINE_LOCATION" android/app/src/main/AndroidManifest.xml && grep -c "NSBluetoothAlwaysUsageDescription" ios/Runner/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BLUETOOTH_SCAN" android/app/src/main/AndroidManifest.xml` returns one match
    - That BLUETOOTH_SCAN line contains `android:usesPermissionFlags="neverForLocation"`
    - `grep "BLUETOOTH_CONNECT" android/app/src/main/AndroidManifest.xml` returns one match
    - `grep "ACCESS_FINE_LOCATION" android/app/src/main/AndroidManifest.xml` returns one match
    - `grep "NSBluetoothAlwaysUsageDescription" ios/Runner/Info.plist` returns one match
    - The &lt;application&gt; block in AndroidManifest.xml is unchanged (android:label="inclinometer" still present)
    - NSBluetoothPeripheralUsageDescription is NOT present in Info.plist
  </acceptance_criteria>
  <done>AndroidManifest.xml declares BLUETOOTH_SCAN (neverForLocation), BLUETOOTH_CONNECT, ACCESS_FINE_LOCATION, plus legacy API &lt;30 entries; Info.plist has NSBluetoothAlwaysUsageDescription</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android manifest → OS | Permission declarations are build-time; OS enforces them at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | AndroidManifest.xml | mitigate | BLUETOOTH_SCAN uses neverForLocation — prevents OS from treating BLE scan as a location data source |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — config file edits only |
</threat_model>

<verification>
After both tasks:
- `flutter test` must remain green (no Dart changes in this plan)
- `grep "minSdk = 24" android/app/build.gradle.kts` outputs one line
- `grep "compileSdk = 35" android/app/build.gradle.kts` outputs one line
- `grep "BLUETOOTH_SCAN" android/app/src/main/AndroidManifest.xml` outputs one line with neverForLocation
- `grep "NSBluetoothAlwaysUsageDescription" ios/Runner/Info.plist` outputs one line
</verification>

<success_criteria>
- BUILD-01: minSdk = 24 hardcoded in build.gradle.kts (no flutter.minSdkVersion delegate)
- BUILD-02: compileSdk = 35 hardcoded in build.gradle.kts (no flutter.compileSdkVersion delegate)
- PERM-01: AndroidManifest.xml declares BLUETOOTH_SCAN, BLUETOOTH_CONNECT, ACCESS_FINE_LOCATION
- PERM-05: ios/Runner/Info.plist contains NSBluetoothAlwaysUsageDescription
- All existing flutter tests pass (no Dart regressions)
</success_criteria>

<output>
Create `.planning/phases/05-app-wiring-platform-config/05-01-SUMMARY.md` when done
</output>
