---
phase: 04-ui-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/providers/device_provider.dart
  - test/ui/scan_screen_test.dart
  - test/ui/instrument_screen_test.dart
autonomous: true
requirements:
  - INST-05
  - INST-06

must_haves:
  truths:
    - "ConnectionNotifier exposes sendCommand(int commandByte) that delegates to BleManager.sendCommand"
    - "test/ui/ directory contains scaffold test files with the correct import structure and a passing smoke assertion"
  artifacts:
    - path: "lib/providers/device_provider.dart"
      provides: "sendCommand method on ConnectionNotifier"
      contains: "Future<void> sendCommand"
    - path: "test/ui/scan_screen_test.dart"
      provides: "widget test scaffold for scan screen"
      contains: "scan_screen_test"
    - path: "test/ui/instrument_screen_test.dart"
      provides: "widget test scaffold for instrument screen"
      contains: "instrument_screen_test"
  key_links:
    - from: "lib/ui/instrument_screen.dart"
      to: "connectionNotifierProvider.notifier.sendCommand"
      via: "ElevatedButton.onPressed"
      pattern: "sendCommand\\(kCmdZero"
---

<objective>
Add the missing `sendCommand` method to `ConnectionNotifier` and create the `test/ui/` directory with skeleton widget-test files.

Purpose: `sendCommand` must exist before InstrumentScreen can compile. The test scaffolds establish the `test/ui/` directory and ProviderScope harness pattern so Wave 3 can add assertions without structural setup work.

Output: One method added to `device_provider.dart`; two test files in `test/ui/` with scaffolding passing `flutter test`.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-screens/04-CONTEXT.md
@.planning/phases/04-ui-screens/04-RESEARCH.md
@.planning/phases/04-ui-screens/04-PATTERNS.md
</context>

<interfaces>
<!-- From lib/providers/device_provider.dart — existing ConnectionNotifier action methods (lines 132–151): -->
<!-- Copy this exact delegation shape for sendCommand: -->
<!--   Future<void> connect(String deviceId) async { -->
<!--     await ref.read(bleManagerProvider).connect(deviceId); -->
<!--   } -->
<!--   Future<void> disconnect() async { -->
<!--     await ref.read(bleManagerProvider).disconnect(); -->
<!--   } -->
<!-- From lib/ble/ble_manager.dart line 24: -->
<!--   Future<void> sendCommand(int commandByte); -->
<!-- From test/providers/connection_notifier_test.dart lines 1–22 (harness pattern): -->
<!--   setUpAll(TestWidgetsFlutterBinding.ensureInitialized); -->
<!--   ProviderContainer buildContainer(MockBleManager mock) { ... } -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add sendCommand to ConnectionNotifier</name>
  <files>lib/providers/device_provider.dart</files>
  <read_first>
    - lib/providers/device_provider.dart — read the full file first; insert new method after `disconnect()` on line ~151, before the closing brace of ConnectionNotifier
    - lib/ble/ble_manager.dart — confirm sendCommand(int commandByte) signature on the abstract class
    - .planning/phases/04-ui-screens/04-PATTERNS.md — Pattern section "lib/providers/device_provider.dart — sendCommand addition"
  </read_first>
  <action>
    Insert a new public method `sendCommand(int commandByte)` into `ConnectionNotifier` immediately after the `disconnect()` method (around line 151), before the closing `}` of the class.

    Method signature and doc comment (exact text):
      /// Sends a command byte to the instrument via [BleManager].
      ///
      /// Used by Zero X / Zero Y buttons in InstrumentScreen.
      /// Keeps [BleManager] access out of [lib/ui/] per CLAUDE.md architecture rule.
      Future<void> sendCommand(int commandByte) async {
        await ref.read(bleManagerProvider).sendCommand(commandByte);
      }

    The method must:
    - Be declared `Future<void>` (matches all other action methods)
    - Call `ref.read(bleManagerProvider).sendCommand(commandByte)` — not `ref.watch`
    - Be added INSIDE the `ConnectionNotifier` class body (before its closing `}`)
    - NOT import anything new (bleManagerProvider and BleManager are already in scope)

    Do not modify any other code in the file.
  </action>
  <verify>
    <automated>cd /c/Users/lfaes/OneDrive/VisualStudio/LevelAppMobile && flutter analyze lib/providers/device_provider.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/providers/device_provider.dart` exits 0 with no errors
    - The file contains the exact string `Future<void> sendCommand(int commandByte) async {`
    - The file contains `await ref.read(bleManagerProvider).sendCommand(commandByte);`
    - The `ConnectionNotifier` class body contains exactly 5 public async methods: startScan, stopScan, connect, disconnect, sendCommand
    - No other lines in the file are changed (diff shows only the insertion)
  </acceptance_criteria>
  <done>ConnectionNotifier.sendCommand compiles; flutter analyze reports no issues on the file.</done>
</task>

<task type="auto">
  <name>Task 2: Create test/ui/ scaffold files</name>
  <files>test/ui/scan_screen_test.dart, test/ui/instrument_screen_test.dart</files>
  <read_first>
    - test/providers/connection_notifier_test.dart — read lines 1–30 for the exact import block, setUpAll pattern, and buildContainer helper; replicate this structure in widget test files
    - .planning/phases/04-ui-screens/04-RESEARCH.md — Validation Architecture section (ProviderScope harness pattern)
    - .planning/phases/04-ui-screens/04-PATTERNS.md — test analog section for scan_screen_test.dart and instrument_screen_test.dart
  </read_first>
  <action>
    Create the directory `test/ui/` (if it does not exist) and write two new test files.

    FILE 1: test/ui/scan_screen_test.dart
    Contents:
    - Import block: dart:async, package:flutter/material.dart, package:flutter_riverpod/flutter_riverpod.dart, package:flutter_test/flutter_test.dart, package:inclinometer/ble/mock_ble_manager.dart, package:inclinometer/models/device_state.dart, package:inclinometer/providers/device_provider.dart
    - Note: do NOT import scan_screen.dart yet — that file does not exist until Wave 2. The scaffold must compile without it.
    - setUpAll(TestWidgetsFlutterBinding.ensureInitialized);
    - A single group('ScanScreen — scaffold') block containing one test: test('placeholder — replaced in plan 04', () { expect(true, isTrue); });
    - A top-level comment: // TODO(plan-04): Replace placeholder with real widget tests (SCAN-01 through SCAN-05)

    FILE 2: test/ui/instrument_screen_test.dart
    Contents:
    - Import block: dart:async, package:flutter/material.dart, package:flutter_riverpod/flutter_riverpod.dart, package:flutter_test/flutter_test.dart, package:inclinometer/ble/mock_ble_manager.dart, package:inclinometer/models/device_state.dart, package:inclinometer/providers/device_provider.dart
    - Note: do NOT import instrument_screen.dart yet — same reason as above.
    - setUpAll(TestWidgetsFlutterBinding.ensureInitialized);
    - A single group('InstrumentScreen — scaffold') block containing one test: test('placeholder — replaced in plan 04', () { expect(true, isTrue); });
    - A top-level comment: // TODO(plan-04): Replace placeholder with real widget tests (INST-01 through INST-07, CONN-04)

    Do not add any real assertions or widget pump calls yet — that is Wave 3 work. The only goal is that both files compile and flutter test test/ui/ passes.
  </action>
  <verify>
    <automated>cd /c/Users/lfaes/OneDrive/VisualStudio/LevelAppMobile && flutter test test/ui/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test/ui/scan_screen_test.dart` exists and contains the string `ScanScreen — scaffold`
    - `test/ui/instrument_screen_test.dart` exists and contains the string `InstrumentScreen — scaffold`
    - `flutter test test/ui/` exits 0; both placeholder tests pass
    - Neither file imports scan_screen.dart or instrument_screen.dart (those files don't exist yet)
    - Both files contain `TestWidgetsFlutterBinding.ensureInitialized`
    - Full suite `flutter test` remains green (no regressions in test/ble/ or test/providers/)
  </acceptance_criteria>
  <done>flutter test test/ui/ shows 2 tests passed; flutter test shows all prior tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget → notifier | User gesture calls sendCommand; notifier owns BleManager access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | sendCommand route in UI | accept | sendCommand delegates through ConnectionNotifier — BleManager never exposed directly to UI; architecture constraint enforced by code structure |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 4 — package legitimacy gate not applicable |
</threat_model>

<verification>
flutter analyze lib/providers/device_provider.dart — no errors
flutter test test/ui/ — 2 tests pass
flutter test — full suite green (all prior phases unaffected)
</verification>

<success_criteria>
- sendCommand method present in ConnectionNotifier with correct delegation to bleManagerProvider
- test/ui/ directory exists with two compilable scaffold files
- All existing tests remain green
</success_criteria>

<output>
Create .planning/phases/04-ui-screens/04-01-SUMMARY.md when done
</output>
