---
phase: 01-data-models-protocol-parser
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/ble/ble_manager.dart
  - lib/ble/mock_ble_manager.dart
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "abstract class BleManager declares all 8 members: scanResults, connectionStatus, statePackets streams; startScan, stopScan, connect, disconnect, sendCommand methods; dispose"
    - "MockBleManager implements BleManager and compiles without 'missing concrete implementation' warnings"
    - "ble_manager.dart has zero flutter_blue_plus imports"
    - "mock_ble_manager.dart has zero flutter_blue_plus imports"
    - "MockBleManager.dispose() does not throw (empty method body)"
    - "All other MockBleManager methods throw UnimplementedError with 'Phase 2:' message"
  artifacts:
    - path: "lib/ble/ble_manager.dart"
      provides: "abstract class BleManager interface contract"
      contains: "abstract class BleManager"
    - path: "lib/ble/mock_ble_manager.dart"
      provides: "Phase 1 stub — compile target only, no behavior"
      contains: "class MockBleManager implements BleManager"
  key_links:
    - from: "lib/ble/ble_manager.dart"
      to: "lib/models/device_state.dart"
      via: "import"
      pattern: "import.*models/device_state"
    - from: "lib/ble/mock_ble_manager.dart"
      to: "lib/ble/ble_manager.dart"
      via: "import"
      pattern: "import.*ble/ble_manager"
---

<objective>
Create the BLE abstraction layer: the abstract class BleManager (the interface contract) and MockBleManager (the Phase 1 compile-target stub). Neither file contains flutter_blue_plus imports — the architecture isolation boundary is enforced here.

Purpose: BleManager is the seam that makes the WP1→WP2 swap a single-line change. MockBleManager in Phase 1 only needs to satisfy the Dart compiler ("no missing concrete implementations") — actual behavior is Phase 2.

Output: lib/ble/ble_manager.dart with the canonical abstract class; lib/ble/mock_ble_manager.dart with UnimplementedError stubs for all methods.
</objective>

<execution_context>
@C:\Users\luke\OneDrive\Claude\get-shit-done\workflows\execute-plan.md
@C:\Users\luke\OneDrive\Claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-models-protocol-parser/01-CONTEXT.md
@.planning/phases/01-data-models-protocol-parser/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-data-models-protocol-parser/01-02-SUMMARY.md
</context>

<interfaces>
Consumed types (from Plan 02 — already exist):
  From lib/models/device_state.dart:
    enum ConnectionStatus { idle, scanning, connecting, connected, disconnecting, disconnected, error }
    class DeviceState { final double angleX; final double angleY; final int battery; }
    class ScannedDevice { final String id; final String name; final int rssi; }

Types defined in this plan and consumed by Plan 04:
  From lib/ble/ble_manager.dart:
    abstract class BleManager {
      Stream<ScannedDevice> get scanResults;
      Stream<ConnectionStatus> get connectionStatus;
      Stream<List<int>> get statePackets;
      Future<void> startScan();
      Future<void> stopScan();
      Future<void> connect(String deviceId);
      Future<void> disconnect();
      Future<void> sendCommand(int commandByte);
      void dispose();
    }

  From lib/ble/mock_ble_manager.dart:
    class MockBleManager implements BleManager { ... all methods UnimplementedError stubs ... }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/ble/ble_manager.dart — abstract interface</name>
  <files>lib/ble/ble_manager.dart</files>
  <read_first>
    - .planning/phases/01-data-models-protocol-parser/01-PATTERNS.md (section: "lib/ble/ble_manager.dart" — abstract class definition and key implementation notes)
    - .planning/research/ARCHITECTURE.md (section: "BLE Abstraction Pattern — Use an abstract class with two concrete implementations")
    - CLAUDE.md (Architecture Constraints — "No flutter_blue_plus import in lib/ui/ or lib/providers/"; ble_manager.dart must also have zero FBP imports)
  </read_first>
  <action>
Write `lib/ble/ble_manager.dart` with a single import: `import 'package:inclinometer/models/device_state.dart';`. No flutter_blue_plus import, no Riverpod import, no dart:async import (the abstract declarations only reference Stream/Future which are dart:core types in scope).

Declare `abstract class BleManager` with exactly these 9 members (doc comments are optional but welcome):
- `Stream<ScannedDevice> get scanResults;`
- `Stream<ConnectionStatus> get connectionStatus;`
- `Stream<List<int>> get statePackets;`
- `Future<void> startScan();`
- `Future<void> stopScan();`
- `Future<void> connect(String deviceId);`
- `Future<void> disconnect();`
- `Future<void> sendCommand(int commandByte);`
- `void dispose();`

Do not add any concrete methods, fields, or constructors to BleManager itself — it is a pure interface. Do not add `isMock` flag or any implementation. RealBleManager is WP2 — it does not appear in this file.
  </action>
  <verify>
    <automated>dart analyze lib/ble/ble_manager.dart</automated>
  </verify>
  <done>dart analyze exits 0 on lib/ble/ble_manager.dart. File contains abstract class BleManager with exactly 9 declared members (3 stream getters + 5 async methods + 1 dispose). No flutter_blue_plus import exists. No concrete method bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/ble/mock_ble_manager.dart — Phase 1 stub</name>
  <files>lib/ble/mock_ble_manager.dart</files>
  <read_first>
    - lib/ble/ble_manager.dart (read the file just created to confirm the exact method signatures to override — do not guess)
    - .planning/phases/01-data-models-protocol-parser/01-PATTERNS.md (section: "lib/ble/mock_ble_manager.dart — Phase 1 stub pattern" with complete UnimplementedError stub)
    - .planning/phases/01-data-models-protocol-parser/01-CONTEXT.md (D-05: MockBleManager scope — "all abstract methods implemented with minimal bodies; throw UnimplementedError or return empty streams")
  </read_first>
  <action>
Write `lib/ble/mock_ble_manager.dart` with two imports: `import 'dart:async';` and `import 'package:inclinometer/ble/ble_manager.dart';` and `import 'package:inclinometer/models/device_state.dart';`.

Declare `class MockBleManager implements BleManager` and override all 9 members from BleManager:

For the 3 stream getters (scanResults, connectionStatus, statePackets): each returns `throw UnimplementedError('Phase 2: <memberName>')`.

For startScan(), stopScan(), connect(String deviceId), disconnect(), sendCommand(int commandByte): each returns `throw UnimplementedError('Phase 2: <methodName>')`. Use arrow syntax: `Future<void> startScan() => throw UnimplementedError('Phase 2: startScan');`

For `dispose()`: implement as an empty method body `void dispose() {}` — it must NOT throw, as dispose() is called on cleanup regardless of connection state.

No StreamController fields, no Timer fields, no state variables — those are Phase 2. This file is the minimal compile target per D-05.
  </action>
  <verify>
    <automated>dart analyze lib/ble/mock_ble_manager.dart</automated>
  </verify>
  <done>dart analyze exits 0 on lib/ble/mock_ble_manager.dart with no "missing concrete implementation" warnings. Class MockBleManager implements BleManager. dispose() has an empty body (does not throw). All other methods throw UnimplementedError with 'Phase 2:' prefix in the message. No flutter_blue_plus import exists.</done>
</task>

</tasks>

<verification>
After Plan 03 completes:
- `dart analyze lib/ble/ble_manager.dart` exits 0
- `dart analyze lib/ble/mock_ble_manager.dart` exits 0
- ble_manager.dart contains abstract class BleManager with 9 members
- mock_ble_manager.dart contains class MockBleManager implements BleManager
- Neither file contains a flutter_blue_plus import
- MockBleManager.dispose() does not throw
</verification>

<success_criteria>
abstract class BleManager defines the full interface contract. MockBleManager compiles without "missing concrete implementation" warnings. The flutter_blue_plus import isolation boundary is clean — neither file imports it. The WP1→WP2 swap remains a single-line change in main.dart.
</success_criteria>

<output>
Create `.planning/phases/01-data-models-protocol-parser/01-03-SUMMARY.md` when done.
</output>
