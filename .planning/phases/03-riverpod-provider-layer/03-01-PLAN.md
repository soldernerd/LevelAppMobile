---
phase: 03-riverpod-provider-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/device_state.dart
  - pubspec.yaml
autonomous: true
requirements:
  - CONN-01
  - CONN-06
  - SYS-01

must_haves:
  truths:
    - "ConnectionStatus.reconnecting exists as an enum value"
    - "wakelock_plus is a direct dependency in pubspec.yaml and resolves without conflict"
  artifacts:
    - path: "lib/models/device_state.dart"
      provides: "ConnectionStatus enum with reconnecting value"
      contains: "reconnecting,"
    - path: "pubspec.yaml"
      provides: "wakelock_plus dependency declaration"
      contains: "wakelock_plus"
  key_links:
    - from: "lib/models/device_state.dart"
      to: "lib/providers/device_provider.dart"
      via: "ConnectionStatus enum import"
      pattern: "reconnecting"
---

<objective>
Prepare the two prerequisites that all Phase 3 providers depend on: (1) add the
`reconnecting` enum value to `ConnectionStatus` so that `ConnectionNotifier` can
transition into that state (D-08), and (2) add `wakelock_plus` to pubspec.yaml
so it is available when `ConnectionNotifier` calls `WakelockPlus.enable/disable`
(D-09, SYS-01).

Purpose: Both Phase 3 plans that follow depend on these artifacts compiling cleanly.
Output: Updated `device_state.dart`, updated and resolved `pubspec.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-riverpod-provider-layer/03-CONTEXT.md
@.planning/phases/03-riverpod-provider-layer/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add reconnecting to ConnectionStatus enum</name>
  <files>lib/models/device_state.dart</files>
  <read_first>
    - lib/models/device_state.dart — read the full file before editing; the enum is lines 4–12; append reconnecting after error
  </read_first>
  <action>
    Open lib/models/device_state.dart. In the ConnectionStatus enum (lines 4–12), append the value `reconnecting,` as the last entry after `error,`. The final enum must contain all eight values: idle, scanning, connecting, connected, disconnecting, disconnected, error, reconnecting. No other changes to the file.

    Per D-08: `reconnecting` is the state ConnectionNotifier transitions to when the auto-reconnect stub "would" fire; it maps to the amber state chip in Phase 4 (CONN-06).
  </action>
  <verify>
    <automated>flutter analyze lib/models/device_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/device_state.dart` contains the literal string `reconnecting,` inside the enum body
    - `flutter analyze lib/models/device_state.dart` exits 0 with no issues
    - The enum has exactly 8 values: idle, scanning, connecting, connected, disconnecting, disconnected, error, reconnecting (in that order)
    - All other content of device_state.dart (DeviceState class, ScannedDevice class) is unchanged
  </acceptance_criteria>
  <done>ConnectionStatus enum has reconnecting value; flutter analyze clean</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add wakelock_plus dependency to pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml — read the full file to confirm current dependencies block structure before editing
  </read_first>
  <action>
    Run `flutter pub add wakelock_plus` from the project root. This command adds the dependency to pubspec.yaml and runs `flutter pub get` automatically. After the command completes, verify the dependency is present in the `dependencies:` block of pubspec.yaml.

    If `flutter pub add` is unavailable, manually add `wakelock_plus: ^1.6.1` under the `dependencies:` section in pubspec.yaml, then run `flutter pub get`.

    Per SYS-01 and D-09: wakelock_plus is needed for WakelockPlus.enable() / WakelockPlus.disable() calls inside ConnectionNotifier.
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml `dependencies:` block contains `wakelock_plus:` (any version constraint)
    - `flutter pub get` exits 0 with no resolution errors
    - `flutter analyze` exits 0 (no dependency conflicts introduced)
  </acceptance_criteria>
  <done>wakelock_plus in pubspec.yaml, flutter pub get resolves cleanly, flutter analyze clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec.yaml → pub.dev | New dependency fetched from public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | wakelock_plus pub.dev package | accept | Package Legitimacy Audit in RESEARCH.md confirms official Flutter community package; pub.dev verified publisher |
| T-03-SC | Tampering | pub.dev package install | accept | wakelock_plus is [Approved] per RESEARCH.md audit; no checkpoint required |
</threat_model>

<verification>
Run `flutter test` — all existing Phase 1 and Phase 2 tests must remain green after the enum change.
Run `flutter analyze` — no new issues introduced.
</verification>

<success_criteria>
- ConnectionStatus enum has 8 values including reconnecting
- wakelock_plus is in pubspec.yaml and resolves via flutter pub get
- flutter test (full suite) exits 0
- flutter analyze exits 0
</success_criteria>

<output>
Create `.planning/phases/03-riverpod-provider-layer/03-01-SUMMARY.md` when done
</output>
