---
phase: 04-ui-screens
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/ui/scan_screen.dart
  - main.dart
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCAN-05
  - INST-01

must_haves:
  truths:
    - "ScanScreen renders with a FAB, an AppBar with scan-state chip, and a device list"
    - "FAB icon is bluetooth_searching when idle and stop when scanning"
    - "Device list filters unnamed devices and shows name + RSSI icon + dBm for named devices"
    - "Tapping a list tile calls connectionNotifierProvider.notifier.connect(device.id)"
    - "App compiles and launches with MockBleManager via ProviderScope override in main.dart"
    - "When connectionNotifierProvider reaches connected state, Navigator.push brings InstrumentScreen into view"
  artifacts:
    - path: "lib/ui/scan_screen.dart"
      provides: "ScanScreen ConsumerWidget"
      exports: ["ScanScreen"]
    - path: "main.dart"
      provides: "Standalone Phase 4 entry point with MockBleManager override"
      contains: "bleManagerProvider.overrideWithValue"
  key_links:
    - from: "lib/ui/scan_screen.dart"
      to: "connectionNotifierProvider"
      via: "ref.watch / ref.read(notifier)"
      pattern: "ref\\.watch\\(connectionNotifierProvider\\)"
    - from: "lib/ui/scan_screen.dart"
      to: "scanResultsProvider"
      via: "ref.watch"
      pattern: "ref\\.watch\\(scanResultsProvider\\)"
---

<objective>
Build `ScanScreen` — the root screen shown on app launch — and a temporary `main.dart` standalone entry point for Phase 4 development.

Purpose: Delivers all five scan-screen requirements (SCAN-01 through SCAN-05) and the navigation entry point to InstrumentScreen (INST-01). The `main.dart` is explicitly temporary; Phase 5 replaces it entirely with go_router.

Output: `lib/ui/scan_screen.dart` (ConsumerWidget), `main.dart` (Phase 4 standalone).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-screens/04-CONTEXT.md
@.planning/phases/04-ui-screens/04-RESEARCH.md
@.planning/phases/04-ui-screens/04-PATTERNS.md
@.planning/phases/04-ui-screens/04-UI-SPEC.md
@.planning/phases/04-ui-screens/04-01-SUMMARY.md
</context>

<interfaces>
<!-- From lib/providers/device_provider.dart: -->
<!--   final connectionNotifierProvider = NotifierProvider<ConnectionNotifier, ConnectionStatus>(...); -->
<!--   final scanResultsProvider = Provider<List<ScannedDevice>>(...); -->
<!--   class ConnectionNotifier: startScan(), stopScan(), connect(String deviceId), disconnect(), sendCommand(int commandByte) -->
<!-- From lib/models/device_state.dart: -->
<!--   class ScannedDevice { final String id; final String name; final int rssi; } -->
<!--   enum ConnectionStatus { idle, scanning, connecting, connected, disconnecting, disconnected, error, reconnecting } -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ScanScreen</name>
  <files>lib/ui/scan_screen.dart</files>
  <read_first>
    - lib/providers/device_provider.dart — full file; understand connectionNotifierProvider, scanResultsProvider shapes
    - lib/models/device_state.dart — ScannedDevice fields (id, name, rssi) and ConnectionStatus enum values (all 8)
    - lib/ble/ble_protocol.dart — confirm no imports needed here (Zero commands only needed on instrument screen)
    - .planning/phases/04-ui-screens/04-UI-SPEC.md — "Scan Screen" component spec (AppBar, FAB, device list, empty state, error state, RSSI icon spec)
    - .planning/phases/04-ui-screens/04-PATTERNS.md — ScanScreen section (imports block, RSSI icon, chip color helper, filtering pattern)
    - .planning/phases/04-ui-screens/04-RESEARCH.md — Pattern 6 (RSSI icon mapping), Anti-Patterns section
  </read_first>
  <behavior>
    - Test: FAB shows Icons.bluetooth_searching when status is idle; shows Icons.stop when status is scanning
    - Test: FAB calls startScan() when idle; calls stopScan() when scanning
    - Test: Device with empty name is not shown in the list (SCAN-03)
    - Test: Device with non-empty name appears as ListTile with name, RSSI icon, and dBm suffix
    - Test: Tapping a ListTile calls connect(device.id) on the notifier
    - Test: Scan state chip label reads "Idle" when idle, "Scanning" when scanning, "Error" when error
  </behavior>
  <action>
    Create lib/ui/scan_screen.dart as a ConsumerWidget. Do NOT use StatefulWidget.

    IMPORTS (exact):
      import 'package:flutter/material.dart';
      import 'package:flutter_riverpod/flutter_riverpod.dart';
      import 'package:inclinometer/models/device_state.dart';
      import 'package:inclinometer/providers/device_provider.dart';

    ARCHITECTURE RULES:
    - Never import flutter_blue_plus (CLAUDE.md prohibition)
    - Never call ref.read(bleManagerProvider) directly in UI (use connectionNotifierProvider.notifier)
    - ref.watch at top of build(); ref.read(notifier) only inside callbacks

    CLASS: ScanScreen extends ConsumerWidget. Constructor: const ScanScreen({super.key}).

    PROVIDER READS in build():
      final status = ref.watch(connectionNotifierProvider);
      final devices = ref.watch(scanResultsProvider)
          .where((d) => d.name.isNotEmpty)
          .toList();

    NAVIGATION: When status reaches ConnectionStatus.connected, push InstrumentScreen.
    Use a post-frame callback to avoid calling Navigator during build:
      ref.listen(connectionNotifierProvider, (prev, next) {
        if (next == ConnectionStatus.connected) {
          Navigator.of(context).push(
            MaterialPageRoute(builder: (_) => const InstrumentScreen()),
          );
        }
      });
    Import InstrumentScreen: import 'package:inclinometer/ui/instrument_screen.dart';
    Do NOT auto-pop on disconnect (D-10) — per CONTEXT.md, the instrument screen stays open.

    SCAFFOLD structure:
    - backgroundColor: const Color(0xFF121212)
    - appBar: AppBar with
        backgroundColor: const Color(0xFF1E1E1E)
        title: const Text('Scan', style: TextStyle(fontSize: 18))
        bottom: PreferredSize wrapping left-padded Chip for scan state (compact VisualDensity)
          Chip backgroundColor from _chipColor(status)
          label: Text(_chipLabel(status), style: TextStyle(fontSize: 13, color: Colors.white))
          visualDensity: VisualDensity.compact
          padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 0)
    - floatingActionButton: FloatingActionButton
        backgroundColor: scanning state → Colors.red[400]; else → const Color(0xFF1E88E5)
        tooltip: scanning → 'Stop Scan'; else → 'Start Scan'
        onPressed: scanning → ref.read(connectionNotifierProvider.notifier).stopScan()
                   else     → ref.read(connectionNotifierProvider.notifier).startScan()
        child: Icon(scanning ? Icons.stop : Icons.bluetooth_searching)
        Show FAB only when status is idle, disconnected, scanning, or error — hide when connecting/connected/disconnecting/reconnecting (FAB is null for those states)
    - body:
        If status == ConnectionStatus.error: show error body text below the list (or centered if list empty)
          Text('Could not start scan. Check Bluetooth is enabled.',
               style: TextStyle(fontSize: 13, color: Colors.white70),
               textAlign: TextAlign.center)
          Wrap in Padding(horizontal: 16dp)
        If devices.isEmpty (and not error): centered Column with
          Icon(Icons.bluetooth_disabled, size: 48, color: Colors.white54)
          SizedBox(height: 12)
          Text('No devices found', style: TextStyle(fontSize: 16, color: Colors.white54))
          SizedBox(height: 8)
          Text('Start scanning to discover devices.', style: TextStyle(fontSize: 13, color: Colors.white38))
        If devices.isNotEmpty: ListView.builder
          Each ListTile:
            leading: Icon(_rssiIcon(device.rssi), color: Colors.white70, size: 20)
            title: Text(device.name, style: TextStyle(fontSize: 16, color: Colors.white))
            trailing: Text('${device.rssi} dBm', style: TextStyle(fontSize: 13, color: Colors.white70))
            onTap: () => ref.read(connectionNotifierProvider.notifier).connect(device.id)
            tileColor: const Color(0xFF1E1E1E)

    PRIVATE HELPERS (top-level functions or static, outside the class):

    IconData _rssiIcon(int rssi) — exact thresholds from UI-SPEC:
      >= -60 → Icons.signal_wifi_4_bar
      >= -75 → Icons.network_wifi_3_bar
      >= -85 → Icons.network_wifi_2_bar
      else   → Icons.network_wifi_1_bar

    Color _chipColor(ConnectionStatus status) — exhaustive switch, exact hex values from UI-SPEC:
      idle          → const Color(0xFF757575)
      scanning      → const Color(0xFF1E88E5)
      connecting    → const Color(0xFFFFA000)
      connected     → const Color(0xFF43A047)
      reconnecting  → const Color(0xFFF57F17)
      disconnecting → const Color(0xFFD32F2F)
      disconnected  → const Color(0xFFD32F2F)
      error         → const Color(0xFFD32F2F)

    String _chipLabel(ConnectionStatus status) — exhaustive switch:
      idle          → 'Idle'
      scanning      → 'Scanning'
      connecting    → 'Connecting…'
      connected     → 'Connected'
      reconnecting  → 'Reconnecting…'
      disconnecting → 'Disconnecting…'
      disconnected  → 'Disconnected'
      error         → 'Error'
  </action>
  <verify>
    <automated>cd /c/Users/lfaes/OneDrive/VisualStudio/LevelAppMobile && flutter analyze lib/ui/scan_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze lib/ui/scan_screen.dart exits 0 with no errors or warnings
    - File contains exactly the class `ScanScreen extends ConsumerWidget`
    - File does NOT contain any import of flutter_blue_plus
    - File does NOT contain ref.read(bleManagerProvider) (would be CLAUDE.md violation)
    - File contains `ref.watch(connectionNotifierProvider)` in build()
    - File contains `ref.watch(scanResultsProvider)` in build()
    - File contains `.where((d) => d.name.isNotEmpty)` (SCAN-03 filter)
    - File contains `Icons.bluetooth_searching` and `Icons.stop` (FAB icon toggle)
    - File contains `_rssiIcon` helper with all 4 threshold branches
    - _chipColor switch covers all 8 ConnectionStatus values (Dart exhaustive switch enforces this at compile time)
  </acceptance_criteria>
  <done>ScanScreen compiles cleanly; flutter analyze reports no issues; all required UI elements are present per the acceptance criteria above.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 4 standalone main.dart</name>
  <files>main.dart</files>
  <read_first>
    - main.dart — read the existing file first to understand its current content before overwriting
    - .planning/phases/04-ui-screens/04-RESEARCH.md — Pattern 7 (standalone Phase 4 entry point) and code example
    - .planning/phases/04-ui-screens/04-PATTERNS.md — main.dart section (override wiring pattern from test/providers/connection_notifier_test.dart)
    - test/providers/connection_notifier_test.dart — lines 15–22, ProviderScope override shape
  </read_first>
  <action>
    Replace the current main.dart with a Phase 4 temporary standalone entry point.

    The file header MUST include this comment:
      // main.dart — Phase 4 temporary entry point.
      // Replaced entirely in Phase 5 by the go_router + permission-handler version.
      // Do NOT add go_router, permission_handler, or platform wiring here.

    IMPORTS (exact):
      import 'package:flutter/material.dart';
      import 'package:flutter_riverpod/flutter_riverpod.dart';
      import 'package:inclinometer/ble/mock_ble_manager.dart';
      import 'package:inclinometer/providers/device_provider.dart';
      import 'package:inclinometer/ui/scan_screen.dart';

    main() function:
      void main() {
        runApp(
          ProviderScope(
            overrides: [
              bleManagerProvider.overrideWithValue(MockBleManager()),
            ],
            child: MaterialApp(
              title: 'Inclinometer',
              theme: ThemeData.dark(),
              home: const ScanScreen(),
              debugShowCheckedModeBanner: false,
            ),
          ),
        );
      }

    No other code. No go_router. No permission_handler. No WidgetsFlutterBinding.ensureInitialized (not needed for basic runApp). This exact minimal form is intentional — Phase 5 replaces the entire file.
  </action>
  <verify>
    <automated>cd /c/Users/lfaes/OneDrive/VisualStudio/LevelAppMobile && flutter analyze main.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - main.dart compiles cleanly; flutter analyze exits 0
    - File contains `bleManagerProvider.overrideWithValue(MockBleManager())`
    - File contains `home: const ScanScreen()`
    - File contains the Phase 4 temporary comment at the top
    - File does NOT contain go_router, permission_handler, or Navigator.push at the top level
    - flutter test (full suite) remains green — main.dart changes must not break existing tests
  </acceptance_criteria>
  <done>main.dart compiles; flutter analyze clean; flutter test full suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → notifier | FAB taps and ListTile taps are the only input surfaces; both dispatch through connectionNotifierProvider.notifier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | device list display | accept | Device names and RSSI are BLE advertisement data; no PII; display is the intended purpose |
| T-04-03 | Tampering | bleManagerProvider override in main.dart | accept | WP1 only; MockBleManager does not touch real BLE hardware; Phase 5 replaces with real manager |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages — package legitimacy gate not applicable |
</threat_model>

<verification>
flutter analyze lib/ui/scan_screen.dart — no errors
flutter analyze main.dart — no errors
flutter test — full suite green (all phases)
</verification>

<success_criteria>
- ScanScreen renders all required states: idle (empty + FAB), scanning (device list), error (error text)
- Device list filters unnamed devices; named devices show name + RSSI icon + dBm
- Tapping a device calls connect(device.id) through the notifier
- main.dart launches the app with MockBleManager via ProviderScope
- No flutter_blue_plus import anywhere in lib/ui/
</success_criteria>

<output>
Create .planning/phases/04-ui-screens/04-02-SUMMARY.md when done
</output>
