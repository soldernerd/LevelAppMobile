---
phase: 01-data-models-protocol-parser
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - lib/providers/device_provider.dart
  - lib/main.dart
  - test/ble/ble_protocol_test.dart
autonomous: true
requirements: [PROT-01, PROT-02, PROT-03, PROT-04, ARCH-01, ARCH-02]

must_haves:
  truths:
    - "flutter test test/ble/ble_protocol_test.dart exits 0 (all 3 tests green)"
    - "flutter analyze exits 0 on the entire lib/ tree with no issues"
    - "bleManagerProvider is defined in lib/providers/device_provider.dart, not in lib/main.dart"
    - "lib/main.dart imports bleManagerProvider from lib/providers/device_provider.dart"
    - "No flutter_blue_plus import exists anywhere in lib/"
    - "Round-trip test uses closeTo(value, 1e-4) for float assertions — not equals()"
  artifacts:
    - path: "lib/providers/device_provider.dart"
      provides: "bleManagerProvider — Provider<BleManager> stub"
      contains: "bleManagerProvider"
    - path: "lib/main.dart"
      provides: "ProviderScope entry point with MockBleManager override"
      contains: "ProviderScope"
    - path: "test/ble/ble_protocol_test.dart"
      provides: "Round-trip parse/encode unit tests for StatePacket"
      contains: "StatePacket"
  key_links:
    - from: "lib/main.dart"
      to: "lib/providers/device_provider.dart"
      via: "import bleManagerProvider"
      pattern: "import.*providers/device_provider"
    - from: "lib/main.dart"
      to: "lib/ble/mock_ble_manager.dart"
      via: "import MockBleManager"
      pattern: "import.*ble/mock_ble_manager"
    - from: "test/ble/ble_protocol_test.dart"
      to: "lib/ble/ble_protocol.dart"
      via: "import StatePacket"
      pattern: "import.*ble/ble_protocol"
---

<objective>
Wire the final three files of Phase 1: the bleManagerProvider stub in lib/providers/device_provider.dart, the updated lib/main.dart that imports from it, and the unit test file that proves the protocol parser is correct.

Purpose: This plan closes the loop — device_provider.dart avoids a Phase 3 refactor (per RESEARCH.md Pitfall 4), main.dart becomes fully resolvable so flutter analyze passes cleanly on the whole project, and the round-trip unit tests provide automated verification of PROT-01 through PROT-04.

Output: lib/providers/device_provider.dart with bleManagerProvider; lib/main.dart updated to import from device_provider.dart; test/ble/ble_protocol_test.dart with 3 passing unit tests.
</objective>

<execution_context>
@C:\Users\luke\OneDrive\Claude\get-shit-done\workflows\execute-plan.md
@C:\Users\luke\OneDrive\Claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-models-protocol-parser/01-CONTEXT.md
@.planning/phases/01-data-models-protocol-parser/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-data-models-protocol-parser/01-03-SUMMARY.md
</context>

<interfaces>
All consumed types (from Plans 02 and 03 — already exist):

From lib/models/device_state.dart:
  enum ConnectionStatus { idle, scanning, connecting, connected, disconnecting, disconnected, error }
  class DeviceState { final double angleX; final double angleY; final int battery; }
  class ScannedDevice { final String id; final String name; final int rssi; }

From lib/ble/ble_protocol.dart:
  const String kServiceUuid, kStateCharUuid, kCommandCharUuid;
  const int kCmdZeroX = 0x01, kCmdZeroY = 0x02;
  class StatePacket {
    static DeviceState parse(List<int> bytes)
    static List<int> encode(double ax, double ay, int battery)
  }

From lib/ble/ble_manager.dart:
  abstract class BleManager { ... 9 members ... }

From lib/ble/mock_ble_manager.dart:
  class MockBleManager implements BleManager { ... UnimplementedError stubs ... }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/providers/device_provider.dart and finalize lib/main.dart</name>
  <files>lib/providers/device_provider.dart, lib/main.dart</files>
  <read_first>
    - lib/main.dart (read the current stub created in Plan 01 Task 3 — verify the import paths match what this task will finalize)
    - .planning/phases/01-data-models-protocol-parser/01-PATTERNS.md (section: "lib/providers/device_provider.dart — bleManagerProvider stub" and section: "lib/main.dart — Phase 1 minimal stub")
    - .planning/research/ARCHITECTURE.md (section: "Inject via ProviderScope override" — canonical ProviderScope.overrides pattern)
    - .planning/phases/01-data-models-protocol-parser/01-RESEARCH.md (section: "Common Pitfalls — Pitfall 4" explaining why bleManagerProvider lives in device_provider.dart, not main.dart)
  </read_first>
  <action>
Create `lib/providers/` directory and write `lib/providers/device_provider.dart`. The file imports `package:flutter_riverpod/flutter_riverpod.dart` and `package:inclinometer/ble/ble_manager.dart`. It declares exactly one thing: `final bleManagerProvider = Provider<BleManager>((ref) { throw UnimplementedError('bleManagerProvider must be overridden at root'); });`. No other providers, no Notifier classes — those are Phase 3. The file is intentionally ~5 lines.

Then update `lib/main.dart` to its final Phase 1 form. The 4 required imports are: `package:flutter/material.dart`, `package:flutter_riverpod/flutter_riverpod.dart`, `package:inclinometer/ble/mock_ble_manager.dart`, `package:inclinometer/providers/device_provider.dart`. The `main()` function calls `runApp(ProviderScope(overrides: [bleManagerProvider.overrideWith((_) => MockBleManager())], child: const MaterialApp(home: Scaffold(body: Center(child: Text('Phase 1'))))))`. bleManagerProvider is imported from device_provider.dart, not redefined in main.dart.

ARCH-02 constraint: no flutter_blue_plus import anywhere in lib/. Verify this holds.
  </action>
  <verify>
    <automated>dart analyze lib/providers/device_provider.dart lib/main.dart</automated>
  </verify>
  <done>dart analyze exits 0 on both files. lib/providers/device_provider.dart contains bleManagerProvider as Provider<BleManager>. lib/main.dart imports bleManagerProvider from providers/device_provider.dart and wires ProviderScope with MockBleManager override. No flutter_blue_plus import exists in either file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/ble/ble_protocol_test.dart and run all tests</name>
  <files>test/ble/ble_protocol_test.dart</files>
  <read_first>
    - .planning/phases/01-data-models-protocol-parser/01-PATTERNS.md (section: "test/ble/ble_protocol_test.dart — round-trip test structure" with exact test names and matcher values)
    - .planning/phases/01-data-models-protocol-parser/01-RESEARCH.md (section: "Common Pitfalls — Pitfall 3" on float32 precision and why closeTo is required; section: "Validation Architecture — Phase Requirements → Test Map")
    - lib/ble/ble_protocol.dart (read to confirm the exact constant names and method signatures before writing the test file)
  </read_first>
  <behavior>
    - Test "encode then parse round-trips float values": StatePacket.encode(12.345, -0.678, 72) → StatePacket.parse(bytes) → angleX within closeTo(12.345, 1e-4), angleY within closeTo(-0.678, 1e-4), battery equals(72)
    - Test "parse asserts on wrong packet length": StatePacket.parse([0, 1, 2]) throws isA<AssertionError>()
    - Test "encode produces exactly 9 bytes": StatePacket.encode(0.0, 0.0, 100).length equals(9)
    - Test "command constants have correct values": kCmdZeroX equals(0x01), kCmdZeroY equals(0x02)
    - Test "UUID constants are non-empty strings": kServiceUuid.isNotEmpty, kStateCharUuid.isNotEmpty, kCommandCharUuid.isNotEmpty
  </behavior>
  <action>
Create `test/ble/` directory and write `test/ble/ble_protocol_test.dart`. Import `package:test/test.dart`, `package:inclinometer/ble/ble_protocol.dart`, and `package:inclinometer/models/device_state.dart`.

Write a `main()` function with a single `group('StatePacket', ...)` containing these 5 tests:

1. Round-trip test: encode(12.345, -0.678, 72) then parse. Assert angleX with `closeTo(12.345, 1e-4)`, angleY with `closeTo(-0.678, 1e-4)`, battery with `equals(72)`. MUST use closeTo, NOT equals, for float fields — float64→float32→float64 loses precision (RESEARCH.md Pitfall 3).

2. Assert on wrong length: `expect(() => StatePacket.parse([0, 1, 2]), throwsA(isA<AssertionError>()))`.

3. Encode length: `expect(StatePacket.encode(0.0, 0.0, 100).length, equals(9))`.

4. Command constants: `expect(kCmdZeroX, equals(0x01))` and `expect(kCmdZeroY, equals(0x02))` in a single test or two separate tests.

5. UUID non-empty: `expect(kServiceUuid.isNotEmpty, isTrue)`, same for kStateCharUuid and kCommandCharUuid.

After writing the file, run `flutter test test/ble/ble_protocol_test.dart` and confirm all tests pass. Then run `flutter analyze` on the full project and confirm it exits 0 with no issues.

Perform a final architecture check: run `grep -r "flutter_blue_plus" lib/` and confirm no matches (zero flutter_blue_plus imports anywhere in lib/).
  </action>
  <verify>
    <automated>flutter test test/ble/ble_protocol_test.dart</automated>
  </verify>
  <done>flutter test exits 0 with all tests passing. flutter analyze exits 0 with no issues on the full project. grep -v "^#" lib/**/*.dart (or equivalent) confirms zero flutter_blue_plus imports in lib/. test/ble/ble_protocol_test.dart contains at minimum 5 test assertions covering round-trip, length assertion, byte count, command constants, and UUID constants.</done>
</task>

</tasks>

<verification>
After Plan 04 completes — full Phase 1 gate:

1. `flutter test test/ble/ble_protocol_test.dart` exits 0 (all tests green)
2. `flutter test` exits 0 (full suite — no widget_test.dart exists to fail)
3. `flutter analyze` exits 0 with no issues
4. lib/providers/device_provider.dart contains bleManagerProvider as Provider<BleManager>
5. lib/main.dart imports bleManagerProvider from providers/device_provider.dart
6. No flutter_blue_plus import exists anywhere in lib/
7. Phase 1 ROADMAP success criteria check:
   - SC1: StatePacket.parse() test passes (9-byte round-trip) — verified by test suite
   - SC2: kCmdZeroX == 0x01 and kCmdZeroY == 0x02 — verified by constant test
   - SC3: kServiceUuid, kStateCharUuid, kCommandCharUuid defined as non-empty strings — verified by UUID test
   - SC4: abstract class BleManager declares full interface; MockBleManager compiles — verified by flutter analyze
   - SC5: No widget file and no flutter_blue_plus import in lib/ui/ or lib/providers/ — verified by grep + analyze
</verification>

<success_criteria>
All 5 Phase 1 ROADMAP success criteria are satisfied. flutter test and flutter analyze both exit 0. bleManagerProvider lives in lib/providers/device_provider.dart (avoiding Phase 3 refactor). The flutter_blue_plus import isolation boundary is clean across the entire lib/ tree.
</success_criteria>

<output>
Create `.planning/phases/01-data-models-protocol-parser/01-04-SUMMARY.md` when done.
</output>
