---
phase: 01-data-models-protocol-parser
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/models/device_state.dart
  - lib/ble/ble_protocol.dart
autonomous: true
requirements: [PROT-01, PROT-02, PROT-03, PROT-04]

must_haves:
  truths:
    - "ConnectionStatus enum has exactly 7 values: idle, scanning, connecting, connected, disconnecting, disconnected, error"
    - "DeviceState and ScannedDevice implement == and hashCode using Object.hash (not XOR)"
    - "StatePacket.parse() accepts List<int> and returns DeviceState using ByteData.sublistView(Uint8List.fromList(bytes))"
    - "StatePacket.encode() produces exactly 9 bytes in [float32LE, float32LE, uint8] layout"
    - "kCmdZeroX == 0x01 and kCmdZeroY == 0x02 as top-level int constants"
    - "kServiceUuid, kStateCharUuid, kCommandCharUuid are non-null non-empty String constants"
  artifacts:
    - path: "lib/models/device_state.dart"
      provides: "DeviceState, ScannedDevice, ConnectionStatus"
      contains: "enum ConnectionStatus"
    - path: "lib/ble/ble_protocol.dart"
      provides: "StatePacket, UUID constants, command constants"
      contains: "class StatePacket"
  key_links:
    - from: "lib/ble/ble_protocol.dart"
      to: "lib/models/device_state.dart"
      via: "import"
      pattern: "import.*models/device_state"
---

<objective>
Create the two pure Dart foundation files: the typed data models (device_state.dart) and the binary protocol parser (ble_protocol.dart). Both files have zero Flutter or BLE package imports and are unit-testable in isolation.

Purpose: These files are the stable API contract that every subsequent phase depends on. Defining them first ensures Plans 03 and 04 can write against known types and constants.

Output: lib/models/device_state.dart with DeviceState, ScannedDevice, and ConnectionStatus; lib/ble/ble_protocol.dart with StatePacket.parse(), StatePacket.encode(), UUID constants, and command constants.
</objective>

<execution_context>
@C:\Users\luke\OneDrive\Claude\get-shit-done\workflows\execute-plan.md
@C:\Users\luke\OneDrive\Claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-models-protocol-parser/01-CONTEXT.md
@.planning/phases/01-data-models-protocol-parser/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-data-models-protocol-parser/01-01-SUMMARY.md
</context>

<interfaces>
The following types are defined in this plan and are consumed by Plans 03 and 04.

From lib/models/device_state.dart (after this plan):
  enum ConnectionStatus { idle, scanning, connecting, connected, disconnecting, disconnected, error }

  @immutable
  class DeviceState {
    final double angleX;
    final double angleY;
    final int battery;
    const DeviceState({required this.angleX, required this.angleY, required this.battery});
    // == and hashCode via Object.hash(angleX, angleY, battery)
  }

  @immutable
  class ScannedDevice {
    final String id;
    final String name;
    final int rssi;
    const ScannedDevice({required this.id, required this.name, required this.rssi});
    // == and hashCode via Object.hash(id, name, rssi)
  }

From lib/ble/ble_protocol.dart (after this plan):
  const String kServiceUuid     = '0000XXXX-0000-1000-8000-00805f9b34fb';
  const String kStateCharUuid   = '0000YYYY-0000-1000-8000-00805f9b34fb';
  const String kCommandCharUuid = '0000ZZZZ-0000-1000-8000-00805f9b34fb';
  const int kCmdZeroX = 0x01;
  const int kCmdZeroY = 0x02;

  class StatePacket {
    static DeviceState parse(List<int> bytes)
    static List<int> encode(double ax, double ay, int battery)
  }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/models/device_state.dart</name>
  <files>lib/models/device_state.dart</files>
  <read_first>
    - .planning/phases/01-data-models-protocol-parser/01-PATTERNS.md (section: "lib/models/device_state.dart" — full DeviceState, ScannedDevice, and ConnectionStatus patterns with exact field names and equality implementation)
    - .planning/research/ARCHITECTURE.md (section: "File responsibilities" row for device_state.dart, and "Connection state machine in Riverpod" for the enum values)
  </read_first>
  <behavior>
    - ConnectionStatus enum has exactly these 7 variants in this order: idle, scanning, connecting, connected, disconnecting, disconnected, error
    - DeviceState has fields: angleX (double), angleY (double), battery (int); all final; const constructor
    - DeviceState.== compares all 3 fields; DeviceState.hashCode uses Object.hash(angleX, angleY, battery)
    - ScannedDevice has fields: id (String), name (String), rssi (int); all final; const constructor
    - ScannedDevice.== compares all 3 fields; ScannedDevice.hashCode uses Object.hash(id, name, rssi)
    - Both classes annotated @immutable; import package:flutter/foundation.dart for @immutable only
    - No BLE imports, no Riverpod imports
  </behavior>
  <action>
Create `lib/models/` directory and write `lib/models/device_state.dart`. The file contains three declarations in this order: the ConnectionStatus enum, then DeviceState class, then ScannedDevice class.

For ConnectionStatus: declare as `enum ConnectionStatus { idle, scanning, connecting, connected, disconnecting, disconnected, error }` — all 7 values, no additional methods needed in Phase 1.

For DeviceState and ScannedDevice: implement `==` using the `identical(this, other) || other is T && field == other.field && ...` pattern. Use `Object.hash(a, b, c)` for hashCode — never XOR. Both classes are `@immutable` with `const` constructors and all-`final` fields.

The only import allowed is `import 'package:flutter/foundation.dart';` (for the `@immutable` annotation). No dart:async, no BLE packages, no Riverpod.
  </action>
  <verify>
    <automated>dart analyze lib/models/device_state.dart</automated>
  </verify>
  <done>dart analyze exits 0 on lib/models/device_state.dart. File contains: enum ConnectionStatus with exactly 7 variants; class DeviceState with 3 fields and Object.hash hashCode; class ScannedDevice with 3 fields and Object.hash hashCode. No flutter_blue_plus import exists in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lib/ble/ble_protocol.dart</name>
  <files>lib/ble/ble_protocol.dart</files>
  <read_first>
    - .planning/phases/01-data-models-protocol-parser/01-PATTERNS.md (section: "lib/ble/ble_protocol.dart" — full StatePacket pattern with the mandatory Uint8List.fromList conversion and exact byte offsets)
    - .planning/research/ARCHITECTURE.md (section: "Parsing implementation" — canonical code with UUID constants and StatePacket class)
    - .planning/phases/01-data-models-protocol-parser/01-RESEARCH.md (section: "Common Pitfalls — Pitfall 3" for float32 precision; "Anti-Patterns" for ByteData.sublistView requirement)
  </read_first>
  <behavior>
    - kCmdZeroX is top-level const int with value 0x01
    - kCmdZeroY is top-level const int with value 0x02
    - kServiceUuid, kStateCharUuid, kCommandCharUuid are top-level const String with placeholder UUID values (non-empty strings in UUID format)
    - StatePacket.parse([0,0,0,0,0,0,0,0,72]) returns DeviceState(angleX: 0.0, angleY: 0.0, battery: 72)
    - StatePacket.encode(12.345, -0.678, 72) produces exactly 9 bytes
    - StatePacket.parse(StatePacket.encode(12.345, -0.678, 72)).angleX is within 1e-4 of 12.345
    - StatePacket.parse([0,1,2]) throws AssertionError (wrong length)
  </behavior>
  <action>
Create `lib/ble/` directory and write `lib/ble/ble_protocol.dart`. The file imports `dart:typed_data` and `package:inclinometer/models/device_state.dart` only — no flutter_blue_plus import ever.

Declare 5 top-level constants in this order: `kServiceUuid`, `kStateCharUuid`, `kCommandCharUuid` (all const String with placeholder BLE UUID format strings: '0000XXXX-0000-1000-8000-00805f9b34fb' style), then `kCmdZeroX = 0x01` and `kCmdZeroY = 0x02` (both const int).

Write class StatePacket with two static methods:

`parse(List<int> bytes)` — assert bytes.length == 9 with message 'State packet must be 9 bytes, got ${bytes.length}', then `final bd = ByteData.sublistView(Uint8List.fromList(bytes));`, return `DeviceState(angleX: bd.getFloat32(0, Endian.little), angleY: bd.getFloat32(4, Endian.little), battery: bytes[8])`.

`encode(double ax, double ay, int battery)` — `final bd = ByteData(9); bd.setFloat32(0, ax, Endian.little); bd.setFloat32(4, ay, Endian.little); bd.setUint8(8, battery); return bd.buffer.asUint8List().toList();`

CRITICAL: do NOT use `ByteData.sublistView(bytes)` where bytes is `List<int>` — it will not compile. The `Uint8List.fromList(bytes)` conversion is mandatory before passing to `sublistView`.
  </action>
  <verify>
    <automated>dart analyze lib/ble/ble_protocol.dart</automated>
  </verify>
  <done>dart analyze exits 0 on lib/ble/ble_protocol.dart. File contains: kServiceUuid, kStateCharUuid, kCommandCharUuid as const String; kCmdZeroX == 0x01 and kCmdZeroY == 0x02 as const int; class StatePacket with static parse() and static encode(). parse() uses ByteData.sublistView(Uint8List.fromList(bytes)). No flutter_blue_plus import exists in the file.</done>
</task>

</tasks>

<verification>
After Plan 02 completes:
- `dart analyze lib/models/device_state.dart` exits 0
- `dart analyze lib/ble/ble_protocol.dart` exits 0
- lib/models/device_state.dart contains ConnectionStatus enum with 7 variants
- lib/ble/ble_protocol.dart contains kCmdZeroX, kCmdZeroY, kServiceUuid, kStateCharUuid, kCommandCharUuid, class StatePacket
- Neither file contains a flutter_blue_plus import
</verification>

<success_criteria>
Both pure Dart foundation files compile cleanly. ConnectionStatus has all 7 states. StatePacket.parse() and encode() are the inverse of each other and use the mandatory ByteData.sublistView(Uint8List.fromList(bytes)) pattern. All constants are defined with correct values.
</success_criteria>

<output>
Create `.planning/phases/01-data-models-protocol-parser/01-02-SUMMARY.md` when done.
</output>
